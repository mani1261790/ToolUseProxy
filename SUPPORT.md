# v0.2の対応範囲

現在のbranchは開発版v0.2.0-alpha.1です。macOS / Linux、Python 3.11–3.12、認証済みCodex CLIを対象とします。CLI 0.153.4で独立した構造化判定を確認しました。Windowsの新判定経路は未検証です。

ToolCall記録、LLMによる意味依存推定、外部通信の分類、到達性による遮断、ライブのログUIを実装しています。複合ToolCall内の情報の混在、セッションを越える派生ファイル、見えない外部通信は未解決です。

判定不能や時間切れは警告して継続します。操作が進んだことは安全の証明ではありません。モデルの見逃し・誤検出があります。現在のDesktop実Hookでの受入確認と広い精度評価は別途必要です。

旧I/O比較、外部分類rule worker、予測・早期停止、最終回答の書換え、自動整理は新しい通常実行経路には含めません。研究用コードは履歴・比較のため残しますが、新製品の機能とは扱いません。

既存の登録DBは読み継げます。manifestのみの環境や古いDB schemaは自動移行しません。元データを保った移行確認が必要です。管理者が停止したworkspaceはHook・CLI・ログUIから使い続けられない境界を維持します。
