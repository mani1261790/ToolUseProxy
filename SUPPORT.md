# 対応範囲と既知の制限

[紹介](README.md) · [導入](QUICKSTART.md) · [仕組み](docs/設計/エッジ生成と送信判定.md) · [非公開の報告](SECURITY.md)

この文書はv0.2系の通常実行経路を説明します。2026年9月25日時点の公開版は `v0.2.0-alpha.16` です。開発中の機能やv0.1の実績と混同しないでください。

## 確認した環境

| 環境 | 状況 |
| --- | --- |
| macOS・Codex Desktop | alpha.16で、承認を求める設定の実HookとGitHub送信を確認。原文と派生物を遮断し、独立した案内の送信を確認 |
| Linux・Python 3.11 / 3.12 | CIで製品テスト、配布物の生成・検証を実施。上記Desktopリハーサルと同じ実機確認を意味しない |
| macOS・Python 3.12 | CIで配布パッケージの起動を確認 |
| Windows | 新しい判定経路の実機受入は未完了 |

認証済みのCodex CLIと、プラグインに対応したCodexが必要です。独立判定の検証に使用したCLIは0.153.4です。詳しい条件と証拠は[alpha.16検証](docs/運用/alpha16検証.md)を参照してください。

## 検査する範囲

ToolUseProxyが扱うのは、CodexのHookに届くToolCallです。入力やツールの仕様に明示された外部送信を先に分類し、送信内容と来歴を検査します。明示された複合コマンドやインラインコードも分類対象です。

ファイル名で実行するスクリプトの内部、Gitのフック、子プロセス、ローカルサービスの転送まで追跡して通信を発見する仕組みではありません。`local` は「この入力に明示された外部送信がない」という分類であり、OSによる通信禁止の保証ではありません。

Hosted Web SearchなどHookに届かないツールや、実行中プロセスへの再検査されない追加入力には、技術的に介入できません。保護情報や派生内容をこれらの経路へ入力しないでください。

## 情報の由来と内容一致

ToolCallの入力・出力、観測できたファイルの版、モデルが推定した依存関係を使います。モデルがエッジを見落とした場合、経路探索が正しくても検出できません。要約・翻訳・計算・符号化などを推定対象に含めていますが、任意の変換への検出保証ではありません。

独立したDLP検査は、解決できた送信内容と保護内容の特徴的なバイト列の完全一致を調べます。短い断片や単調な文字列は対象にならず、言い換えの判定でもありません。一致しないことだけで許可せず、来歴も検査します。

保護指定は通常、プロジェクト内のファイル全体です。判定時の登録一覧と、記録開始後の読取り属性を照合します。あとから登録しても記録済みの経路には反映できますが、記録開始前の作業や観測されなかった変換を復元することはできません。ファイル名の変更や別セッションへの持ち越しも、対応する資源の版を観測できる範囲に限ります。

## 判定未完了と実行の扱い

| 状態 | 現行版の動作 |
| --- | --- |
| ローカル操作と分類できた | 来歴の詳細解析を後に回し、Codex本来の承認手順へ戻る |
| 保護内容の一致、または保護情報源への経路を検出した | 実行前に遮断し、根拠を記録する |
| 必要な検査が完了し、一致も保護経路もない | このプラグインでは遮断せず、Codex本来の承認手順へ戻る |
| 修復可能なモデル応答・一時的な障害 | 同じHook内で再試行し、再利用できる完了済みの判定は保持する |
| 必要な証拠の欠落、継続する障害、待機期限の超過など | 判定未完了として実行を保留する。Hookの `deny` を使うため、実際に操作は進まない |

**判定未完了を警告だけで通す旧方針は廃止しています。** 一方、期限切れによる保留をなくすという要件は、任意の入力・障害について達成できた状態ではありません。保留は流出検出の成功でも、単なる画面上のラベルでもありません。[判定完了と復旧](docs/設計/判定完了と復旧.md)で残る経路を説明しています。

解析を再開しても、元のツール操作を自動実行しません。再要求時に最新の資源と登録を検査し、Codex本来の権限承認も維持します。

## 初期設定・データ・管理

初期設定は記録を始める操作です。保護登録が0件なら記録だけを行い、モデルによる保護判定は行いません。登録操作そのものでは本文を読みませんが、後の内容一致検査では保護ファイルを読み取ります。

記録はローカルに保存され、判定のために入力・出力や取得した証拠をモデルへ渡します。ログは暗号化DBではありません。[プライバシー](PRIVACY.md)を確認してください。

通常の解除は独立した管理者承認を使います。ただし、エージェントがOSの管理者権限を取得できる環境や、Hookを任意に外せる環境まで防ぐ製品ではありません。[管理者導入](docs/運用/Unsetup管理者導入.md)と[実機確認の範囲](docs/運用/Unsetup実機受入.md)を参照してください。

## 現行製品に含めないもの

旧I/O類似度エンジン、旧外部分類ルールの学習、予測・早期停止、最終回答の書換え、旧自動整理は、v0.2の通常実行経路に含めません。[旧版コード](legacy/README.md)と[過去の文書](docs/履歴/README.md)は経緯を調べるための資料です。
