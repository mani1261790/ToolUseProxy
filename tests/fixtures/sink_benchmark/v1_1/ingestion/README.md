# Sink benchmark v1.1 ingestion fixtures

This nested dataset follows source-ingestion schema v3. File-backed Bash
scenarios intentionally remain `coarse_fallback` in the production static
extractor; the independent benchmark resolver evaluates their current file
snapshot without changing production policy.
