# Sink benchmark v1 ingestion fixtures

This nested dataset follows the source-ingestion v3 schema. It exists so the
benchmark can reuse the production parser, source chunker, adapters, runtime
lineage, finding detector, and policy evaluator.
