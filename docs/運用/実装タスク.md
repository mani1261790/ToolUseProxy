# 実装タスク計画

ToolUseProxy の次に実装する内容を整理する作業計画です。

現在の研究フェーズは第2段階「情報流から漏えい候補を検知する」から第3段階「情報流出をStopする」へ移る境界です。情報流グラフ、外部sink、final answer sink、最小漏えい検知、policy decisionへの変換は動いています。次に優先するのは、PolicyDecisionをCodex Hookの返却形式へ変換し、実行時介入へ接続できる形にすることです。

## 現在の到達点

実装済みの基盤は次の通りです。

- Codex Hooks から `PreToolUse` / `PostToolUse` / `Stop` の payload を受け取る
- Hook payload を event、artifact、artifact fragment として SQLite に保存する
- artifact fragment 間の exact、substring、shingle 類似度から情報流 edge を作る
- JSON container rootとPostToolUse側の複製入力を除外し、session/turn境界内のcanonical fragmentだけを比較する
- protected source を後から graph へ binding する
- source binding を起点に lineage を伝播する
- Filesystem adapter により、ファイル read/write を resource version 経由の edge として表現する
- 実Codexの`apply_patch`をAdd/Update/Delete/Moveへ分解し、成功したPostToolUseだけをresource versionへ反映する
- Bashの静的な`cat`とredirectionをresource versionへ接続し、protected pathから同一command内のexternal sinkまで実行前lineageを作る
- Search / Bash / MCP adapter により、外部へ出る可能性があるtool inputを `sink_candidate` として表現する
- Codex final answer adapter により、最終応答を `sink_candidate: final_answer` として表現する
- `trace_lineage.py` と `export_graph.py` により、source から sink candidate までの経路を確認する

未実装の大きな部分は、policy による判定調整、介入です。

## グラフ設計の前提

情報流グラフは有向グラフとして扱います。

理由は、情報流には「どこから来て、どこへ行ったか」という向きがあるためです。たとえば次の2つは意味が異なります。

```text
.env -> tool_output -> search_query
search_query -> tool_output -> .env
```

無向グラフにすると、source、経由点、sink の区別が失われ、漏えい検知の根拠を説明できなくなります。

この graph は広い意味ではナレッジグラフと呼べますが、研究上は `information-flow graph` または `provenance graph` と呼ぶ方が正確です。目的は一般的な知識表現ではなく、観測された tool I/O の由来と伝播経路を追跡することだからです。

現時点では動的ナレッジグラフとして複雑な更新モデルを作る必要はありません。event と edge を時系列で保存し、再解析によって lineage を更新できれば、第1段階の MVP として十分です。

## 次に実装すること

### 1. 情報流経路を表示する CLI

実装済みです。

目的は、DB に保存された `lineage_assignments` と edge を使って、ある node がどの protected source から来た可能性があるのかを人間が確認できるようにすることです。

作るもの:

- `scripts/trace_lineage.py`
- node ID を指定して source までの経路を逆向きに表示する機能
- source ID を指定して下流 node を一覧する機能
- edge の relation、method、score、reason を表示する機能
- artifact fragment の短い preview を表示する機能

完了条件:

- source から resource version、tool output、tool input までの経路を表示できる
- 分岐した情報流を別々の経路として確認できる
- source と無関係な node は経路なしとして表示される

### 2. 情報流グラフの export

実装済みです。

目的は、情報流グラフを目視できる形に変換し、研究報告やデバッグで使えるようにすることです。

作るもの:

- `scripts/export_graph.py`
- Graphviz DOT 出力
- Mermaid 出力
- JSON 出力
- source lineage に含まれる node だけを出すオプション
- session、tool、source、sink候補で絞り込むオプション

完了条件:

- GitHub Markdown 上で Mermaid graph として確認できる
- Graphviz で画像化できる DOT を出力できる
- source 由来の経路と孤立 node を見分けられる

### 3. SinkCandidate基盤

実装済みです。

目的は、外部へ出る可能性がある場所を `sink_candidate` node として graph 上に表現することです。漏えい判定は後続の `detect_leaks.py` で扱います。

作るもの:

- `SinkCandidate` model
- `sink_candidates` table
- `AdapterResult` が sink candidate を返せる仕組み
- `trace_lineage.py` と `export_graph.py` での `sink_candidate` 表示
- SinkCandidate と adapter の関係を説明する [外部流出候補.md](../設計/外部流出候補.md)

完了条件:

- source lineage が `sink_candidate` まで伝播できる
- graph export で外部sink候補を他のnodeと区別できる
- 検知側が `node_kind = sink_candidate` を共通の出口として扱える

### 4. Search adapter

実装済みです。

目的は、web search query を `sink_candidate: external_search` として graph 上に表現することです。

作るもの:

- `hook_monitor/analysis/adapters/search.py`
- search query fragment から `sink_candidate: external_search` への `submitted_to` edge
- tool 名や semantic role から search query を認識する処理
- Search adapter の説明を [アダプター.md](../設計/アダプター.md) に追記

完了条件:

- protected source 由来の情報が search query に到達した場合、external search sink までlineageとして辿れる
- search query が外部 sink 候補として graph export に表示される
- local README 書き込みと external search を graph 上で区別できる

### 5. sink 分類の拡張

実装済みです。

目的は、第2段階の漏えい検知に進む前に、Search以外のtool input/outputも出口の種類として分類できるようにすることです。

現在扱う sink 種別:

- `external_search`
- `external_http_request`
- `external_file_transfer`
- `external_git_publish`
- `external_package_publish`
- `external_deploy`
- `external_message`
- `external_api_call`

作るもの:

- `hook_monitor/analysis/adapters/bash.py`
- `hook_monitor/analysis/adapters/mcp.py`
- Bash / MCP adapterから sink 種別を付与する仕組み
- graph export で sink 種別をlabelに反映する機能

完了条件:

- 同じ source lineage でも、local write と external search を区別できる
- 第2段階で「source lineage が external sink に到達したか」を判定できる準備ができている

### 6. 最小の漏えい検知

実装済みです。

目的は、情報流グラフを使って、指定 source 由来の情報が外部 sink に到達したかを判定することです。

作るもの:

- `hook_monitor/analysis/leak_detection.py`
- `scripts/detect_leaks.py`
- source lineage と sink 分類を使った最小判定
- text / JSON output
- path score に基づく severity
- `trace_lineage.py --node sink_candidate:<id>` へつなぐ導線

完了条件:

- `.env -> search query` のような危険経路を検知できる
- `private.py -> local README` のような許可したい経路を区別できる
- 判定結果に source、経由 edge、sink、score、reason が含まれる
- sink がない analysis run でも `findings=0` として正常終了する

### 7. Policy判断

実装済みです。

目的は、漏えいfindingを `allow` / `warn` / `redact` / `block` / `continue_review` の判断へ変換することです。Codex Hookへの接続は後続作業とし、まずoffline CLIで判断結果を確認できるようにします。

作るもの:

- `hook_monitor/policy/models.py`
- `hook_monitor/policy/engine.py`
- `scripts/evaluate_policy.py`
- `LeakFinding` から `PolicyDecision` への変換
- text / JSON output

完了条件:

- external sink に source lineage が到達する場合に `block` または `warn` を判断できる
- final answer sink に source lineage が到達する場合に `continue_review` または `warn` を判断できる
- local write など許可すべき操作を過剰に止めない設計になっている
- 判断理由を人間が trace graph で確認できる

### 8. Codex Hook接続

一部実装済みです。Stop hookの`continue_review`に加え、実payloadを確認済みのBashだけを対象に、PreToolUseから`permissionDecision: deny`を返すopt-in接続を追加しました。どちらも同じsession差分graphを使い、現在eventのsinkだけを評価します。Hookが返したdeny / warn / continue_reviewは`policy_decisions`へ保存します。

Stop / PreToolUse runtimeはsession単位の差分解析を共有します。初回、detector変更、source manifest変更、cursor不整合時だけ同一sessionをfull rebuildし、通常は未処理sequenceのcanonical fragment、adapter edge、source binding、lineageだけを追加します。source設定が変わらない場合はprotected source本文を再読込しません。

目的は、PolicyDecisionをCodex Hookの返却形式へ変換し、実行時のwarn / block / continueに接続することです。

作るもの:

- `hook_monitor/policy/codex_output.py`
- `hook_monitor/policy/explanation.py`
- `hook_monitor/runtime/stop_policy.py`
- `hook_monitor/runtime/pre_tool_policy.py`
- `hook_monitor/runtime/policy_audit.py`
- `scripts/list_policy_decisions.py`
- `PreToolUse` の `permissionDecision: deny`
- `PermissionRequest` の `decision.behavior: deny`
- `PostToolUse` の追加context
- `Stop` の `decision: block` による追加確認ターン

完了条件:

- `block` をCodexのhook stdout JSONへ変換できる
- `continue_review` をStop hookの継続指示へ変換できる
- Codexに未サポートの `permissionDecision: ask` へ依存しない
- Hook接続前にoffline policy evaluationで判断を検証できる
- Stop hookで最終応答を記録した直後に `final_answer` の判断を返せる
- Hook reasonで根拠を追えるが、raw protected textは出さない
- Stop hookで返した判断をDBに保存し、decision idで追跡できる
- Bash external sinkを実CodexのPreToolUseで実行前denyできる
- deny時にHTTP副作用とPostToolUseが発生しないことを確認できる

残り:

- Search / MCPの実payloadを観測してadapterとの対応を確認する
- Search / MCPのPreToolUse捕捉範囲を確定する
- `PermissionRequest`を実接続する必要があるか評価する
- `LeakFinding` と `PolicyDecision` を別テーブルへ分けるか決める

## 次セッションの推奨作業

次セッションでは、次の順で実装します。

1. Search / MCPの実payloadを観測し、捕捉可能範囲を確定する
2. MCP external sinkのPreToolUse接続を追加する
3. apply_patch/Bashのoperation単位fragmentとsnapshot captureを設計する
4. 複数workspaceのsource / cursor分離を設計する
5. ローカルembeddingによる意味類似候補を評価する

Search / Bash / MCP / Codex final answer adapterで作った`sink_candidate`を使う最小漏えい検知、policy判断、Stop継続、Bash PreToolUse denyまで動いています。次はSearch / MCPの実payloadを観測し、Codexが実際にPreToolUseで捕捉できる経路を確定します。
