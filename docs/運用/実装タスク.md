# 現行実装タスク

この文書は、2026-09-06時点の実装順序と完了条件を示す正本です。研究上の理由と評価モデルは[現行研究方針](../研究/現行研究方針.md)、日々の担当と状態は[GitHub Issues](https://github.com/mani1261790/ToolUseProxy/issues)と[Project #1](https://github.com/users/mani1261790/projects/1)を参照してください。次のセッションで作業を再開するときは[次セッション引き継ぎ](次セッション引き継ぎ.md)から読み始めます。

## 現在地

開発状況は、次の2層に分けて扱います。

1. **Codex対応は完了**: install、5 Hook、workspace setup、現在セッションのHook到達確認、Hookから見えるlocal toolの実行前block、update / rollback / removeまで、`0.1.0-alpha.12`で一連の動作を確認しました。さらに`0.1.0-alpha.13`で、未設定projectを巻き込まない利用対象判定を公開しました。
2. **ToolUseProxy本体の検出精度は改善中**: protected sourceから外部sinkまでの検出を、より多くのcommand、payload、変換、session境界で正しく行うことが現在の中心課題です。

2026-08-30にはPromptPredictionで、設定済みであっても現在のタスクへPreToolUseが届いた証拠を得られず、protected curlが起動する問題が見つかりました。alpha.12では、設定や過去の成功だけを`active`の根拠にせず、verification command自身のPreToolUse eventをsession、Plugin版、Hook定義hashへ結び付けるよう修正しました。その後の新しいPromptPredictionタスクでは、5件・12チャンクの保護対象を維持したまま、現在セッションへのPreToolUse到達と実行前blockを確認しています。

したがって、Codex integrationやDesktop Phase Bをもう一度作り直すことは、現在の優先事項ではありません。次は実project pilotから検出の誤りと未対応範囲を測り、製品本体を改善します。

## 完了済み: Codex Plugin integration

`0.1.0-alpha.12`で、次を完了しました。

- installable Codex Pluginと公開alpha配布
- SessionStart、SubagentStart、PreToolUse、PostToolUse、Stopの5 Hook
- workspace単位のsetup、protected source登録、revision確認、値非保持監査
- Hook-visible local toolのpublic allowとprotected flowの実行前deny
- current-invocation Hook healthと`configured_unverified` / `active`の分離
- update、rollback、remove、同一版再install
- fresh Codex Desktop runの全35 checks
- public side effect 1、static / dynamic protected side effect各0、実行前block各1、raw exposure 0
- PromptPredictionの新しいタスクで、現在セッションのHook到達とprotected flowのblockを確認

この完了は、Hookから見えるlocal toolについてのものです。Hosted Web SearchなどHookへ配送されない操作、`write_stdin`による実行中processへの追加入力、任意programの完全な外部性判定、Windows対応を保証するものではありません。

## P0: 実project pilot（Issue #99）

先行修正: [#107 未設定プロジェクトではPluginの通知・記録・停止を行わない](https://github.com/mani1261790/ToolUseProxy/issues/107)を実装・検証し、`0.1.0-alpha.13`として公開しました。未設定・別project・設定済み・DB故障・起動失敗・旧版からの引継ぎ・同時設定・入れ子の別repositoryを含む全体試験1150件合格、2件スキップ、補助確認1590件合格。対象判定を共通DBの準備確認より先に行い、固定の進捗表示も除去しました。設定済みの故障は安全側の停止を維持します。公開版を現在のCodexへ再導入し、新規Codex実行で設定済みprojectの開始・操作前・操作後・終了の記録と、未設定projectの無表示・無記録を確認済みです。

状態: **In Progress**

保存形式、比較条件、問題分類、GitHub Issueへの接続方法は[実プロジェクト試行の記録・比較・改善提案](../設計/実プロジェクト試行.md)で確定しました。低riskな複数projectで通常利用し、内容を保存・共有せずに、次を測ります。

- public callのfalse block
- protected flowの見逃しと正しいblock
- unknown判定のtool family別分布
- adapter、static analysis、reviewed ruleの寄与
- Hook latency、復旧性、説明の分かりやすさ
- raw exposure、workspace混線、stale rule採用、既存block downgradeが0であること

完了条件:

- 2つ以上の実projectで通常利用の結果を記録する
- 問題を閉じた原因番号`externality`、`payload_resolution`、`protected_match`、`lineage`、`coverage_boundary`、`explanation`、`unidentified`に分類する。日本語表示では順に「外部操作の判断」「送信内容の確認」「保護対象との照合」「情報のつながり」「観測範囲」「停止理由の説明」「原因未特定」とする
- 観測された全問題を、同じ問題はまとめたうえで改善提案Issueへつなげる
- 保護値、raw Hook payload、SQLite本文をIssueへ貼らない

実装は次の順序で進めます。

GitHubの担当Issueは順に #101、#102、#103、#104、#105、#106です。各Issueを1つのCodex Goalとして進め、完了条件を確認してから次のGoalへ移ります。#101〜#105はPR #109で統合・完了済みです。次は#106をGoalに設定し、実利用の試行記録とGitHubへの提案送信を明示的に有効化して確認します。alpha.13の全体試験は1150件合格、2件スキップ、補助確認1590件合格です。

1. **公開・完了**: Hookを変えず、集計器と人工データを実装する
2. **公開・完了**: 一般向け初期状態は無効のまま、内容非保持の操作評価を保存する
3. **公開・完了**: 停止確認、後からの確認、訂正履歴を実装する
4. **公開・完了**: タスク終了時の初回・継続比較とHook非可視操作の件数確認を実装する
5. **公開・完了**: 内容検査を通った全問題を、重複させずGitHub Issueへ接続する
6. **未開始**: 2つ以上の低危険度プロジェクトで各20件以上を確認する

各段階で、実装済み、試験済み、未確認、次の作業を分けて報告します。人工データと模擬通信の試験合格は、実プロジェクトでの確認とは分けて扱います。

## P1: sink-first / lineage比較（Issue #36）

direct lexicalとresolved payloadをbaselineにし、lineageを加えた場合にrecallがどれだけ改善するかを比較します。graphを作ること自体ではなく、検出精度、false block、latency、保存量の改善を完了条件にします。

評価方法は[Sink中心の情報流評価計画](../調査/Sink中心の情報流評価計画.md)と[Sink-first比較評価](Sink-first比較評価.md)を参照してください。

## P1: 外部性とdynamic shell（Issues #54、#24）

既知adapterとbounded static analysisで判定できないcallを、単純なcommand名の列挙へ依存せず分類します。

- Codex network proxyのOTLP eventはevaluation-onlyを維持する
- 実行前callbackとtool-use単位の厳密joinが得られるまではproduction denyへ接続しない
- dynamic shellは、sourceから変数、変数からsinkまでの由来を値非保持で追う
- 判断不能なprotected flowはfail closedを維持しつつ、public callの不必要なblockを減らす
- Externality JudgeはHook外の一時的Codex sessionだけで動かし、自動でallow ruleを昇格しない

## P1: sink payload coverage（Issue #38を含む）

外部性を分類できても、実際に送られるbytesを解決できなければ漏えいを確定できません。bounded resolverを追加し、direct comparisonとの差分を測ります。

優先順:

1. file-backed HTTP payloadとTOCTOU境界
2. Git pushのoutgoing commit / blob
3. archive / upload
4. MCP profileの実schema coverage
5. hosted toolのHook非可視境界

## P2: semantic対応（Issue #46）

local semanticをobserve-onlyで評価します。semantic similarityは候補抽出には使えても、因果的な情報流の証明ではありません。独立評価を終えるまでsemantic-onlyの結果を自動blockへ使いません。

## P2: session・Git境界（Issues #37、#39）

task再開、compaction、subagent、branch、worktree、別cloneで、どのidentityとrevisionを引き継ぐべきかをbounded reconcileとして設計します。alpha.11で追加したprotected-source manifest reconcileと、分析session / Git境界のreconcileは別問題として扱います。

## P3: auditと将来の学習（Issue #55）

実利用ログから精度を改善できる評価基盤を作ります。raw protected valueを学習dataや外部serviceへ送らず、集計可能な構造情報、明示的な人間review、holdout評価を使います。学習結果をそのままallowへ接続しません。

## 維持する安全条件

- protected flowを判断不能なexternal / unknown callへ到達させない
- runtime / policy / schema / bounded analysis失敗をallowへ変換しない
- revision、workspace、receiptの不一致を安全側で拒否する
- raw command、path、host、protected valueをExternality Judgeへ渡さない
- broad reusable permissionやsystem-wide interceptionを要求しない
- Hook内でLLMやnetwork応答を待たない
- LLM verdictからallow ruleを自動作成しない
- 既存adapterやstatic blockを新しい仕組みで弱めない

## release方針

文書、Project、Issueの整理だけではPlugin版を上げません。runtime、Hook、skill、manifestなど配布物が実質的に変わり、clean artifact、CI、必要なDesktop gateを通したときにだけ次のalphaを作ります。

Linux / Windows、hosted tool、任意program、数学的な偽陰性ゼロは、証拠なしに保証へ含めません。

## 現在行わないこと

- Codex integrationやDesktop Phase Bの全面的なやり直し
- direct OpenAI API、API key設定、provider fallback
- Hook内でLLM応答を待つこと
- broad reusable permissionやsystem-wide interception
- LLM verdictからallow ruleを自動作成すること
- Codex network OTLP eventをproduction denyへ接続すること
- runtime redactの有効化

## 履歴

2026-08-10までの詳細な完了記録と当時の優先順位は[旧実装タスク](../履歴/運用/実装タスク-2026-08-10.md)へ移しました。過去文書は当時の証拠を残す資料であり、現在の仕様・優先順位の正本ではありません。
