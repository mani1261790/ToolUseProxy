# 次セッション引き継ぎ

更新日: 2026-09-06

## 結論

優先追加: [Issue #107](https://github.com/mani1261790/ToolUseProxy/issues/107)で、未設定projectへのPlugin初期化案内を修正しました。入口と起動scriptの両方で利用対象を確認し、対象外では5 Hookを無出力にします。明示設定時に専用保存領域の`events.db.workspaces/`へprojectごとの印を保存し、DBやPython起動が壊れても設定済みの保護を黙って解除しません。projectごとに分けることで、同時設定による上書きと、一つの印の破損が別projectへ波及することを防ぎます。入れ子になった別のGit repositoryも親の設定を引き継ぎません。全体試験1150件合格、2件スキップ、補助確認1590件合格です。

2026-09-06に`v0.1.0-alpha.13`をGitHub prereleaseとして公開し、同じtreeを`public-alpha`へ反映しました。現在のCodexは公式のGitHub配布元から`tooluseproxy@tooluseproxy`を再導入済みです。設定済みprojectの新規Codex実行では、公開文字列を表示する1コマンドに対して同じ実行番号の`session_start`、`pre_tool_use`、`post_tool_use`、`stop`を各1件DBに記録しました。未設定projectでは、新規Codex起動時にToolUseProxyの案内を出さず、5 Hookの直接確認もすべて無出力・正常終了となり、DBへの追加は0件でした。既存の保存先は継続利用しています。rootの`protected_sources.json`は作業前後で同一です。

旧版は対象一覧を持たないため、DBの明示初期化・設定・保護リストの存在から対象を引き継ぎます。過去の自動観測行だけでは有効化と見なしません。旧DB自体が読めない、対象一覧が壊れた、Hookの作業場所が識別できない、Pythonを起動できない場合は、対象外だと断定できないため安全側の警告・停止を維持します。旧DBと対象一覧の両方を失った場合、以前の設定を復元判定することはできません。2026-09-06の導入時に明示setupを行い、既存の明示設定2件をprojectごとの印へ移行しました。

Codex対応は`0.1.0-alpha.12`で一区切りとなり、未設定projectを巻き込まない修正と実project評価基盤を`0.1.0-alpha.13`で追加しました。次のセッションでは、Plugin導入やDesktop dogfoodをやり直さず、ToolUseProxy本体の実利用確認を進めます。

親Issueは[#99 実project pilotで検出精度とunknown分布を測る](https://github.com/mani1261790/ToolUseProxy/issues/99)です。保存形式、比較条件、問題分類、GitHub Issueへの接続は[実プロジェクト試行の記録・比較・改善提案](../設計/実プロジェクト試行.md)で確定しました。#101〜#105と先行修正#107は実装・検証・公開まで完了しました。次は#106の実利用確認です。試行記録とGitHubへの自動投稿はまだ無効で、実利用20件の収集は始めていません。

## 完了したこと

- PR #97を`main`へmergeし、`v0.1.0-alpha.12`を公開した
- PR #98で同じtreeを`public-alpha`へ昇格した
- Python 3.11 / 3.12 CI、macOS package smoke、reproducible release candidateが合格した
- fresh Codex Desktop gateは35 / 35 checksがtrueだった
- public side effect 1、static / dynamic protected side effect各0、実行前block各1、raw exposure 0を確認した
- install、update、rollback、disable、remove、data retentionを確認した
- PromptPredictionの新規タスクで、登録済み5件・12チャンク、現在のPreToolUse配送、同一セッションの実行前blockを確認した
- Issue #19と`v0.1.0 Public Alpha` milestoneを完了した
- PR #109を`main`へmergeし、`v0.1.0-alpha.13`を再現可能な配布物として公開した
- PR #110でrelease commitと同じtreeを`public-alpha`へ反映した
- 現在のCodexをGitHubの公開配布元からalpha.13へ再導入し、設定済みprojectの記録継続と未設定projectの無表示・無記録を確認した

## 現在のGitHub管理

- [Project #1](https://github.com/users/mani1261790/projects/1): Codex対応完了、Detection Qualityフェーズへ更新済み
- [Detection Quality milestone](https://github.com/mani1261790/ToolUseProxy/milestone/2)
- In Progress: #99、#36
- Todo: #106、#24、#54、#38、#46、#37、#39、#55

優先順は次のとおりです。

1. #99: 実project pilot
2. #36: direct / resolved / lineage比較
3. #54 / #24: externalityと動的shell
4. #38: Git push payload
5. #46: local semantic
6. #37 / #39: session・Git境界とreconcile
7. #55: Audit / ML候補

## 次のセッションで最初にすること

1. 現在のbranch、`git status`、Issue #99と#106、Project #1、現在のGoalを確認する
2. [現行研究方針](../研究/現行研究方針.md)と[実装タスク](実装タスク.md)を読む
3. [実プロジェクト試行の設計](../設計/実プロジェクト試行.md)と2つの判断記録を読む
4. 操作評価表はv8、停止確認と見逃し候補はv9、比較位置と範囲確認はv10、送信待ちと同期設定はv11で実装・公開済み。試行用の記録はまだ有効化していない
5. 一般向け初期状態では記録せず、Maniの設定でだけ有効にする
6. 実装済みの`hook_monitor/runtime/pilot_models.py`を確認し、閉じた項目以外を受け入れないことと、event IDがSHA-256参照へ置き換わることを試験で再確認する
7. 評価保存失敗が許可・停止を変えず、保護処理失敗は安全側の停止を維持することを試験する
8. 実装済み、試験済み、未確認、次の作業を分けて報告する

alpha.13の最終全体試験は1150件合格、2件スキップ、補助確認1590件合格です。保存失敗時も保護停止は維持され、初期状態は無効です。実際のGitHub試験投稿は行っていません。

2026-09-06に公開alpha.13を現在のCodexへ導入しました。未設定projectの新規Codex起動はToolUseProxy案内なし・DB追加0件で、5 Hookの直接確認も無表示でした。設定済みprojectの新規Codex実行は`session_start`、`pre_tool_use`、`post_tool_use`、`stop`を同じ実行番号で各1件記録しました。#106ではこの公開版を使い、人工データによる確認と実利用20件を混ぜないでください。

Codex Pluginの再実装、alpha.12 release gate、alpha.13公開確認、Desktop Phase Bはやり直しません。新しいCodex版でHook surfaceが変わった場合だけ、回帰として別に扱います。

## 作業時の注意

- repository rootの`protected_sources.json`はToolUseProxyが作成した利用者管理ファイルである。削除、編集、commitをしない
- protected source本文、raw Hook payload、URL、host、absolute pathをIssueやCI artifactへ出さない
- semantic-only、LLM verdict、unknown classificationを自動allowへ昇格しない
- Hookへ届かないHosted Web Search、`write_stdin`継続、未検証nested toolを保護済みと表現しない
- unknownにprotected flowがある場合のfail-closedを維持し、検出精度改善で既存blockを弱めない

## 新しいセッションへの開始文

次の文章をそのまま使えます。

> `/Users/mani/Developer/ToolUseProxy`の開発を引き継いでください。最初にbranch、git status、Issue #99と#106、Project #1、`docs/運用/次セッション引き継ぎ.md`、`docs/運用/実装タスク.md`、`docs/研究/現行研究方針.md`を確認してください。`0.1.0-alpha.13`の公開と現在のCodexへの導入は完了済みです。次はIssue #106をGoalに設定し、試行記録を明示的に有効化したうえで、性質の異なる低危険度project 2つ以上から各20件以上の内容非保持記録を得て、自動比較と改善提案Issueへの接続を実確認してください。人工データと実利用を混ぜず、rootの`protected_sources.json`は削除・編集・commitしないでください。`
