# 次セッション引き継ぎ

更新日: 2026-09-18

## 最新の実装状況

- #133はPR #201をmain d8358d4へ統合・Issue/Project完了。最終head 6a507a9の
  CI 35314274355はPython 3.11/3.12・macOS・配布再現性すべて成功。
  CodeRabbitは統合時点でレビュー中。後着コメントは次の継続で確認する。
- #134はPR #202をmain cdf6705へ統合・Issue/Project完了。最終head e4f305dの
  CI 35317149528はPython 3.11/3.12・macOS・配布再現性の4ジョブ成功。
  [基準方式と評価条件](未来予測の基準方式と評価条件.md)の規約はF03前に凍結済み。
- #135はPR #203をmain 21f1f70へ統合・Issue/Project完了。最終head 3374161の
  CI 35318756506はPython 3.11/3.12・macOS・配布再現性すべて成功。
  [研究用モデル](../../research/flow_forecast/README.md)に構成・版・実測・限界を記録。
- #136はPR #205をmain 6787006へ統合・Issue/Project完了。最終head 8df6435の
  CI 35320530828はPython 3.11/3.12・macOS・配布再現性すべて成功。
  [比較結果](将来経路予測の比較結果.md)の結論は評価不能・採用しない。
  独立holdout・生成モデル・未使用性の証跡不足はIssue #204で追跡し、予測停止は導入しない。
- 現在は#137。専用worktree `/private/tmp/tooluseproxy-issue-137`、
  branch `codex/issue-137-record-only-worker`。記録専用Request/Bindingの閉じた契約を
  `research/flow_forecast/recording/contracts.py`に追加。専用SQLite journalと別プロセスworkerも
  実装し記録関連28試験、将来予測関連計151試験成功。
  project/session/操作/保護設定/方針/観測版/世代/モデル変更、期限切れを区別して破棄する。
  無効化/再有効化で世代を更新して古い要求を拒否し、履歴は保持。二重処理、期限切れ、
  中断復旧、入力DB故障、モデル不在、実子プロセスのtimeout、推論中の版変更を検証。
  EventStoreの読取専用adapter・管理者lease連携・CLI/ローカル表示・実環境証拠は未完成。
  workerのread_currentには新しいBindingと選択モデルdigestを返すCurrentを渡す。
  実利用のrecorded_structure入力はF04未達のためout_of_domainで推論しない。
  実機有効化、同一Mac/Codex実行での追加負荷と既存保護の確認は最後へ残す。
  F04の有効性は未実証であり、実装中の接続を製品導入済みや有効性確認済みとはしない。


- #140はmain 48d1da9へ統合。CI 35311887335の4ジョブ成功、ローカル全体1795件成功、
  2 skip、1609 subtests成功。48形式のDocker実通信は公開literal4形式の到達・完了、
  他44形式の実行前拒否、保護到達0、全5ケースの独立対照成立。


- #140の限定Python HTTP形式を保護情報照合へ接続。人工Docker旧新版比較で
  公開送信の誤停止が改善し、保護停止対照も成立。PR #200で統合・Issue完了。
  [検証範囲と証拠](Python固定HTTP送信の検証.md)に対応形式と制約を記載。

- #188はPR #192をmainへ統合済み（3c899c0）。人間の実機受入は最後に残し、Issueはopen。
- #189のPR #193は人工再生画面を作っており、利用者の目的と不一致だったため再open。
  実際のevents.dbへ接続し、ToolCall・I/O・ブロック判定をリアルタイム表示する
  読み取り専用ビューアーへ修正。起動は[展示画面](../exhibition/README.md)。
- #144はPR #194で統合・Issue完了。
- #130はPR #195で統合・Issue完了。実Codex + Dockerで拒否後の別案と上限終了を確認。
  [Codex人工探索](Codex人工探索.md)に、版・受入条件・実証と模擬試験の区別を記載。
- #189のライブDBビューアー修正はPR #196で統合済み。
- #131は新しいDocker環境での再現・短縮・旧新版比較を接続し、
  2操作から1操作への短縮と、旧新版で同じ誤停止が残ることを実通信で確認。
  [再現と修正前後比較](再現と修正前後比較.md)に実行・保存・受入範囲を記載。
  #131はPR #197で統合済み。 #132のIssue連携はPR #198で統合済み。
  続いて#140の誤停止修正、#133〜#138を進める。
  探索だけで「自動探索と改善一式」が完成したとは扱わない。
- #132の専用outbox・共有内容検査・既存Issue対応を実装。
  #140への実投稿は旧新版各1件、再実行は追加0件。
  [人工試験のIssue連携](人工試験のIssue連携.md)に状態と検証範囲を記載。
- PR #197の後着レビューも修正。policy版不一致の比較拒否、根拠なし指紋の拒否、
  短縮完了時の予算表示、CLI既定600秒、古い引き継ぎ指示を回帰試験付きで整理。


## 最新の利用者指示：人間の確認は全機能実装の後へ

- #188の機能実装・CIはPR #192に揃っている。管理者環境での実機承認は未検証として
  [人間による最終確認](人間による最終確認.md)へ残し、後続実装の開始条件にしない。
- #189、#144、#130、#131は統合済み。現在は#137の記録専用処理。次は#138の人工対照試験。実機・導入判断は最後に残す。
- 未検証の項目を合格としてcloseしないが、そのIssueを理由に他の機能実装を止めない。
- #189はPR #196で実DBのライブ表示へ修正済み。#144はPR #194、#130はPR #195、
  #131はPR #197で統合・Issue完了。
- 以下の「#188完了を待つ」記述は旧方針。この最新指示を優先する。

## 以下は#188実装中の経過記録（最新の進め方は上記を優先）

## IssueごとのGoalで順次実装（利用者の追加指示）

- Codex Goalは#188をactiveとして作成済み。各Issueについて実装・検証・CI・
  PR統合を完了し、IssueとProjectを完了にしてから、そのGoalをcompleteにする。
  次のIssueで新しいGoalを作る。未完了のIssueを先送りしてcloseしない。
- 順序: #188 → #189 → #144 → #130 → #131 → #132 → #133〜#138。
  関連#140は自動探索と改善一式の受入条件に照らして扱う。
- #188の続きで、OS管理者所有の独立した状態管理とHookのleaseを実装中。
  `authority_state.py` は対象・世代・操作を照合し、停止中の新規Hook受付を止め、
  既存Hookが残る間はdeactivating、終了後にinactiveへ遷移する。
  元ファイル・検出DB・保護リストは変更しない。旧形式からの暗黙の登録は禁止。
- これは承認機構の完成ではない。管理者用の承認入口、改変不能な導入物、
  ACLを含む実機の権限検証、非同期workerの扱い、再初期化導線がまだ必要。
  通常CLIのapply拒否は維持。実機の管理者領域への導入はしていない。
- PR #192の初回CIは配布再現性・macOS smoke成功、Python 3.11/3.12で失敗。
  3.11は既存externality dogfoodの `hook_p95_budget_exceeded`、3.12は既存redactionの
  32件境界試験でeligible期待に対しrejected。統合前に診断し、合格を確認する。
- 状態管理・Hook連携の対象試験97件成功。保護リストを含まない独立checkoutで
  全体1454件＋補助1606件成功、2件skip。Ruff・compileallも成功。
  redactionの件数境界試験は時計を固定し、時間制限の専用試験を維持した。
  変更後のredaction試験34件＋補助17件も成功。遅延上限は変更していない。
- PR #191の2件の文書指摘を修正し、CI全件成功後に統合済み
  （main: `2ca2a8c`）。PR #192はmain向けDraftへ変更。
  次は管理者用承認入口・導入物・worker連携・再初期化と実機検証を進める。
- 次のGoal継続で、管理者用入口 `authority_admin.py` と単一ファイル生成
  `scripts/build_authority_admin.py` を追加。固定のroot-owned導入先とisolated/no-site
  Pythonを要求し、通常ユーザーの呼出しは拒否する。確認期限120秒、対象のowner・
  canonical path・inode・世代を検査し、取消・期限切れ・差替えを拒否する。
  入力による確認はOS認証とは区別する。中断後はstatus、既存停止処理のdrainはfinish。
- [管理者導入手順](Unsetup管理者導入.md)を追加。管理者の認証情報・root shell・UI・
  sudo委任へエージェントが到達できる構成は不可。Macへの導入は未実施。
  生成物はMac同梱Python 3.9.6のisolated/no-siteでもhelpと未承認拒否を確認。
- `1f6a6ff` 時点のPR #192 CIは全件成功。新しい変更は別commitで検証する。
  次の実装はworker連携とCLIの停止状態表示・再初期化導線。その後、人工対象で
  管理者権限・ACL・実際の人間の承認を確認する。#188は未完了のまま。
- `c2ba1b6` のCIも全件成功。CLIは管理者停止中にDB・保護リストへ触れず状態を返し、
  init/setup/protect等の実行を拒否する。設定は未調査と明示し、未設定とは扱わない。
  activeなCLIの処理中はleaseを保持し、停止との競合時は終了までdrainする。
- 外部分類workerも対象projectのleaseをprovider呼出しと結果保存まで保持する。
  inactive/deactivatingのジョブは変更せず飛ばし、他projectの処理を続ける。
  管理者状態が導入済みなのにworkspace identityを解決できない場合は送信しない。
  対象回帰148件＋補助4件成功後、identity不一致・未登録の拒否試験も追加。
  次はpilot同期と自動整理のworker連携。ここまでは人工fixtureの検証であり、
  実機導入・人間の承認・全worker停止の受入確認ではない。
- pilot比較の準備・同期にも参加project全員のleaseを接続。
  停止projectを含む比較はprepare/outboxを変更せず保留し、他の比較は継続する。
  participant aliasとworkspaceの対応が解決できない場合は送信を拒否する。
  同期中は認証・GitHub照会・送信・ローカル結果保存までleaseを保持する。
  人工fixtureと偽transportによる混在project・同期中停止・無通信・不正identityの
  対象試験60件成功。実GitHubへの試験送信はしていない。
  次は自動整理と複数projectを参照する比較生成の停止境界を確認・接続する。
- 比較生成にも停止境界を追加。停止済みprojectのStopはcoverage取得前に終了する。
  他projectのStopから比較を作る際も停止projectを入力から除外し、参加projectの
  leaseを比較結果のcommitまで保持する。元の観測記録は削除しない。
  3 project中1件停止・残り2件で比較・比較中停止のdrain・観測60件保持を人工試験。
  pilot生成/同期の対象25件とRuff成功。次は自動整理の候補選定と削除の両方へ接続。
  `storage_cleanup.py` の `_retention_candidates` はplan/apply双方で使われ、
  scoped sessionだけでなくsessionなしevent・共有migration backupも扱うため、
  停止projectの履歴をそれらの経路から削除しないことも確認する。
- 自動/手動整理共通のplan/applyへ管理者状態を接続。停止projectのsessionと
  sessionなしeventを候補から除外し、削除transactionのcommitまでactive projectの
  leaseを保持する。所属不明の記録も管理者状態導入時は保持する。
  共有migration backupは過去の参加projectを証明できないため、管理者状態導入時は
  `cleanup_blocked` とし削除保留。現役DBの他projectの期限切れ記録は整理できる。
  停止前の整理計画は停止後にrevision不一致で拒否される。
  人工fixtureの対象78件＋補助3件、Ruff成功。実データの整理・削除はしていない。

## #188 承認設計の着手結果（2026-09-18）

- branch: `codex/issue-188-unsetup-authorization`。PR #191 の branch から分岐し、
  Draft PR #192を作成。承認経路未実装のため#188をcloseするPRではない。
- 開始時のGoalは未設定。新しいGoalは作成していない。
- [承認設計ADR](../adr/0005-Unsetupの承認をエージェントの実行権限から分離する.md)を追加。
  CLI自身の確認番号・TTY・環境変数では承認できない。管理側のコード・状態・
  Hook設定までエージェントの書込権限から分離することを適用の条件とした。
- `unsetup plan` はDB・保護リストを開かない日本語プレビュー。
  現在の有効状態は未調査と明示する。`unsetup apply` は常に拒否し、変更しない。
- #188 は未完了。独立した管理側の承認・適用、同時Hook・再初期化・実機確認が残る。
  故障時の暫定復旧は利用者によるPlugin管理画面の手操作。エージェントは代行しない。
- 検証: 保護リストを含まない独立checkoutで全体1423件＋補助1606件成功、2件skip。
  対象回帰84件＋補助4件、Ruff、compileall、diff checkも成功。
  CI・実利用への導入・人間による解除の実機証拠とは分ける。
- PR #191 はCI全件成功、未統合。レビューに退避削除の証拠の記述と、下記の旧手順
  5〜6の順序矛盾が指摘された。旧手順はこのbranchで修正。退避の実データは調査していない。
- Project #1 の実装順は #188 → #189 → 自動探索と改善一式 → 将来予測 → #190。
  #188をIn Progressへ更新・読み戻し済み。#189はTodo。
  概要readmeには古い順序・alpha.18の状態が残る。
- Pluginは利用者申告どおり無効のまま扱い、設定操作はしていない。
  rootの保護リストは閲覧・編集・削除・stage・commitしていない。

## 最優先の引き継ぎ（2026-09-18）

### 次のセッションで再開する場所

- 作業branch: `codex/exhibition-roadmap-20260918`。文書整理のPR #191は未統合。最初にレビュー・CIと差分を確認する。
- Project #1の「実装順」は設定・読み戻し済み。#188 → #189 → 自動探索と改善一式 → 将来予測 → #190の順を維持する。
- 次の実装対象は#188。ただし利用者から「エージェントにUnsetupを悪用されたら保護が終わる」と指摘があった。正規CLIの構文・対象確認だけでは利用者承認を証明できない。
- Unsetupの変更前計画は読み取り専用にできるが、適用にはエージェントが生成・偽造・再利用できない利用者承認の経路が必要。`--yes`、会話中の自己申告、エージェントが読める確認番号だけで解除可能にしない。実際に利用可能なCodex/OSの承認機構を調べ、未承認・偽装・再送・別projectの拒否を試験する。
- 故障時の人間による解除は必要だが、無条件の管理命令許可とは別。信頼できる承認経路が提供できなければ、適用だけ利用者の手操作にする。同一ユーザー権限の任意shellでローカルファイルを書き換えられる環境では、CLIだけで悪意あるエージェントへの完全な耐性を保証できない。保護境界を明示する。
- Plugin alpha.24は利用者が無効化済み。自動で再有効化しない。今回の依頼は引き継ぎまでで、Unsetup実装はまだ開始していない。
- 新規Goalを作る前に現在のGoalを確認する。旧#176 Goalの残存を、新しい優先順位の全機能完了と取り違えない。

次は **#188 Unsetup → #189 展示画面 → #144/#130/#131/#132 自動探索と改善一式 → #133〜#138 将来予測 → #190 Jev比較**。展示が近いため、固定人工試験の再生で先に展示を成立させ、自動探索の完成を待たない。詳細とデータ整理結果は[最新の実装順](2026-09-18-実装順とデータ整理.md)を参照。

alpha.24の導入・有効状態は9月18日に確認済み。下記のalpha.22無効化・再起動待ちは過去の経緯であり、現在の操作指示に使わない。容量対策の主要実装と初回整理は実施済み。旧v7退避は手動削除、v11退避は専用フォルダーへ移動済み（利用者によるその後の削除は未確認）。現役DBはquick_check合格。

#153/#159の最終実動作確認と自動整理の延期継続、#167/#169は未解決の残件。未確認の保護成功を主張せず、展示前の全件解決を前提にしない。rootのprotected_sources.jsonを閲覧・変更・削除・commitしない。

以下は9月9日までの履歴。作業順は上の最新順を優先する。

9月18日の追記: alpha.24でもProject更新とgit diffの確認がhook_deadline_exceededで実行前に停止した。利用者がPluginを無効化し、enabled=falseと通常操作の復帰を確認した。現在は無効のまま。#188では、判定時間切れ・DB使用中・破損などでも利用者による正規のUnsetupが停止されないことを必須とする。Unsetup追加だけで判定の遅延原因が直るとは扱わない。

## 最新の依頼と計画の状態

容量増加への対策を、自動攻防試験より先に進めています。親Issue #153の下で、#154「変更前計画」、#155「保存重複削減」、#156「30日を過ぎた操作記録の安全な整理」、#157「更新前DBの退避整理」、#158「タスク終了後の自動整理」は完了しました。#158では、初回確認前は無効、タスク終了時のHookは予約だけ、別処理は前回開始から24時間に1回・20作業単位までとしました。DB使用中、別整理中、監視処理が始まった場合は延期または未確定処理を中断します。利用者の実DBでは初回整理まで完了し、操作記録と退避を削除せずDB本体の未使用領域だけを縮小しました。

容量対策は#154 → #155 → #156 → #157 → #158 → #159の順です。現在は#159の最終確認中です。`0.1.0-alpha.22`を公開し、`public-alpha`と現在のCodexへ導入しました。現在のPluginは利用者が無効化した状態を保っており、有効化とCodexの完全再起動後の実経路確認が残っています。設計ではschema v12移行前にも計画と利用者確認を求めていましたが、計画命令はv12だけを受け付け、v11では安全停止しました。実作業では更新前DB全体を残してv12へ先に更新し、その後、削除を伴わない最初の変更前計画を表示しました。設計どおりの順序ではなかったことを隠さず、旧形式のまま移行前計画を出す修正をIssue #169へ分離しています。30日以内の操作記録、改善用フィードバック、利用者評価、設定、保護対象登録は容量だけを理由に削除しません。

### alpha.19の公開・導入と初回計画

- PR #165を統合し、`v0.1.0-alpha.19` prereleaseと7ファイルを公開。公開物のoffline検証とSHA-256照合に合格
- PR #166で`public-alpha`を公開タグと同じ内容へ更新。現在のCodexへalpha.19を導入し、有効状態を確認
- 実利用DBをschema v11からv12へ更新。更新前DB退避を作成し、既存の保護リストは作成・変更していない。ただし移行前確認を実装できず設計順序から外れたため、Issue #169で修正する
- 初回の`storage cleanup plan`は保護リスト読取なし、DB変更0、source変更0、network使用なしで完了
- 30日超で現在削除できる操作記録は0件。詳しい操作記録約1.43 GB、再作成可能な検出用data約1.72 GB、改善用feedback約0.89 MB、長期設定約0.18 MB
- DB本体約4.81 GB、そのうち未使用page約1.66 GB。更新前DB退避は2件・約7.50 GB、管理対象合計は約12.31 GB
- 更新前DB退避2件は、現在版の監視処理確認前なので削除不可。DB縮小には約4.83 GB必要で、計画時の利用可能量約20.65 GBは条件を満たす
- 1回目の計画作成に約11分かかったため、性能改善をIssue #167へ分離しProject #1へ登録
- 利用者は計画を明示確認したが、取り直した2回目の計画作成には約14分かかり、表示直後の自動整理enableが`automatic_cleanup_plan_expired`で安全に拒否された。期限を計画開始時刻から数えていた不具合をIssue #170へ分離し、整理、backup削除、DB縮小は未実施

### alpha.20の修正・公開・初回整理

- Issue #170 / PR #171で、計画の表示完了時刻`reviewed_at`から5分を数え、境界時刻・表示完了時刻・確認番号を一組として照合するよう修正
- `v0.1.0-alpha.20` prereleaseを7ファイルで公開し、PR #172で`public-alpha`を同じtreeへ更新。現在のCodexへ導入し、Plugin版`0.1.0-alpha.20`、CLI版`0.1.0a20`、有効状態を確認
- 全試験1,381件、補助1,606件合格、2件skip。Python 3.11 / 3.12、macOS package、再現可能な配布候補、公開元検査finding 0に合格
- 実DBの変更前計画は、30日超の操作記録0件、削除可能な更新前退避0件、DB整合性正常、縮小用空き容量十分。保護リスト未参照、DB変更0、source変更0、外部通信なし
- 自動整理を有効化し、初回整理を1回実行。操作記録0件、退避0件を削除し、DB本体だけを4,811,567,104 bytesから3,049,226,240 bytesへ縮小。1,762,340,864 bytesを回収
- 縮小後の未使用pageは0 bytes、DB整合性正常。更新前DB退避2件・7,500,648,448 bytesは現在版のHook確認前なので維持
- 自動整理は有効、次回実行可能時刻は2026-09-09 03:23:33 JST。管理対象全体は約10.55 GBで4 GiB基準を超えるため要対応通知が残るが、主因は維持中の更新前DB退避
- alpha.20時点では、#159の残件はCodexを完全に終了・再起動した新しいタスクで、Hook到達、保護対象の実行前停止、通常操作の復帰を確認することだった。現在の確認対象はalpha.22

### alpha.22の時間切れ復旧と導入

- Issue #176 / PR #177で、自動整理の全体調査中に新しいHook操作が始まったら調査を中断し、通常利用を優先するように修正
- DBが未作成、破損、旧形式、または書き込み中でも、正規launcher・現在project・専用領域と厳密一致する管理操作だけを復旧経路として許可。追加shell命令や通信を付けた類似命令は安全停止を維持
- 外部送信先でないと確定したローカル操作は記録を残しつつ重い全体判定を省略。後の外部操作で必要なつながりを失わないよう読取位置は進めない
- 確認用番号の引数順を変えても、正規CLIが受理する並びなら「今回の操作」のHook到達確認を省略しない回帰試験を追加
- Issue #178で、1万候補のオフライン性能試験が共有CI負荷で500〜600 msを揺れる問題を修正。警告上限を1,000 msへ調整しただけで、精度・安全性・再現性の基準は変更していない
- 全体試験1,390件・補助1,606件、追加の類似度試験8件・補助64件、Python 3.11 / 3.12、macOS導入、配布物再現検査に合格
- `v0.1.0-alpha.22`をGitHub事前公開版として7ファイルで公開し、再ダウンロード後の検査に合格。PR #179で`public-alpha`を公開タグと同じ内容へ更新
- 空の分離Codex設定でGitHubからalpha.22を導入できることを確認。現在のCodexもalpha.20からalpha.22へ更新し、旧版cacheを除去。利用者が無効化した状態は維持
- #159の残件は、利用者がPluginを有効化し、Codexを完全に再起動した新しいタスクで、Hook到達、保護対象の実行前停止、通常操作とToolUseProxy管理操作の復帰を確認すること

### #158の完了証拠

- 自動整理は初期状態で無効。変更前計画の表示完了時刻から5分以内で、境界時刻・表示完了時刻・確認番号が一致した場合だけ有効化
- タスク終了時は小さな予約ファイルを作り、別処理を起動してHookを待たせない
- 前回開始から24時間に1回、20作業単位まで。日付変更だけでは再実行しない
- DB使用中、別整理中、監視処理の直後は延期。削除中の監視開始はSQLite処理を中断して未確定変更を元に戻す
- 強制終了時の実行中状態を保持し、次回に失敗通知を残して24時間後に安全に再開
- DB縮小は空き領域が十分な場合だけ行い、監視開始時は中断。終了後に追記領域整理とDB簡易検査を実行
- 2 GiB以上を注意、4 GiB以上を要対応として本文非表示で記録。正常時は通知せず、失敗、4 GiB以上の継続、空き容量不足だけを次回開始時に1回通知
- `status`で前回日時、削除・残件数、回収容量、延期理由、次回実行可能時刻を確認可能
- 全体1380件合格、2件スキップ、補助1603件合格。配布物・アンインストール関連15件合格、1件スキップ
- #158完了時点では、利用者の実DBでの自動整理有効化、削除、DB縮小は未実施。最初の計画確認と適用は#159へ引き継いだ

## 自動攻防試験の完了済み履歴

容量対策を始める前に、[実装計画](自動攻防試験と将来経路予測の実装計画.md)を親Issue #126、子Issue #127〜#138、通常利用開始の確認 #139へ分割しました。通常利用開始の報告地点は完了しています。当時の公開・導入済み版は`0.1.0-alpha.18`でした。#127 → #128 → #129 → #112 → #113に加え、追加修正#143、通常利用開始の確認#139、個別解除と自己ブロック修正#149を完了しました。容量対策の開始時に公開・導入済みだった版は`0.1.0-alpha.19`で、実利用DBはschema v12へ更新済みです。Dockerでの固定実通信11試行と7つの対照確認は[人工試験環境](人工試験環境.md)を参照してください。残る#144と#130以降は、容量対策#159が終わった後に再開します。

PR #141・#142のalpha.16に対し、統合後に届いたレビューで質問の複数項目へ分割した保護文字列の見落としを再現しました。#143 / PR #145で表示全体の照合を加え、PR #146で配布経路へ反映し、alpha.17を公開・導入しました。未修正のalpha.16を通常利用開始の最終版にはしていません。

### alpha.18の完了証拠

- #149 / PR #150で、登録済みの保護対象を1件だけ外す`protect remove plan / apply`を追加
- 元ファイル、ほかの登録、利用者が追加した未知の項目を残し、変更前の保護リストを専用領域へ権限`0600`で保存
- 確認後に保護リストが変わった場合、対象pathや確認番号が異なる場合は変更しない。再送は`already_removed`として安全に終了
- 正規launcher、現在のCLI構文、同じproject、同じ専用領域を満たすローカル管理操作は、ToolUseProxy自身が停止しないよう統一
- 外部通信を行い得る`externality process`と`pilot sync`、追加shell命令、別project、不正な引数は自己操作として許可しない
- 全体1323件合格、2件スキップ、補助1601件合格。Python 3.11 / 3.12、macOS導入確認、再現可能な配布物確認が合格
- `v0.1.0-alpha.18`を公開し、`public-alpha`と公開タグの内容一致を確認。現在のCodexへalpha.18を導入して有効化済み
- 導入済みの実物と人工workspaceで、個別解除、再送、元ファイル保持、他登録保持、非公開backupを確認
- 導入済みHookへ状態確認、設定表示、個別解除確認を入力し、3件とも停止されないことを確認
- repository rootの利用者管理`protected_sources.json`は内容取得、変更、削除、stage、commitをしていない。実際の`README.md`登録も変更していない

現在開いているタスクはPlugin更新前に始まっているため、新しいHookがこのタスクへ読み込まれた証拠にはしません。実利用ではCodexを一度再起動し、新しいタスクから解除対象を指定します。

### alpha.17の完了証拠

- 全体1314件合格、2件スキップ、補助1601件合格。Ruffと必須CIが合格
- 公開元は`6570ea259f1821fbb1be8bcbebc7e7d6398f2003`。公開タグと`public-alpha`は同じ内容
- cleanな専用checkoutでソース監査の指摘0、配布物検証、隔離Codexでの導入・更新・復帰試験が合格
- 公開7ファイルのハッシュがローカル検証済みファイルと一致。導入済み監視処理100ファイルも公開ZIPと一致
- 新しく起動したアプリ同梱CodexでGit一覧と質問表示を許可し、動的な外部操作は実行前に停止
- 同じ実行の確認コマンドが終了0となり、`current_invocation_protected_block_observed`、新版の照合、現在操作のHook到達を確認。開始1、操作前4、操作後3、終了1の記録
- 未設定の新規Git projectは5段階とも終了0・出力0、新しいCodex実行でも通知なし・記録0
- 5 Hookの確認済み状態を維持。信頼確認の回避や信頼設定ファイルの直接編集は行っていない
- root保護リストと、既存2件のworkspace設定revisionは作業前後で同一。実利用DBの削除・作り直しなし

利用者には、Codexを一度再起動し、新しいタスクで普段どおり使うよう報告しました。現在開いている古いタスクが新版へ切り替わったとまでは主張しません。alpha.16の初回確認コマンドで操作後記録を得なかった事例は#116へ記録し、終了時の定型文を成功証拠にしていません。

研究用処理の中断予約・再開・受信設定エラーの追加課題は#144で扱い、#130の自動探索より先に解消します。#130以降の継続運転はまだ起動していません。配布ZIP・wheel・sdistは研究処理を除外しますが、Git経由ではソース一式がコピーされます。通常の監視処理から研究処理を読み込まず、自動起動しないことと、ファイルの不在を区別してください。

### 今回の変更

- #127: ホストの実データ・認証を渡さない通信なしの実行領域を確認
- #128: 人工試験専用の記録、操作前予約、重複防止、途中終了後の未確定状態を実装
- #129: 受信側を独立させた固定HTTP試験を実行。停止判定、受信対照、故障注入を別集計
- #112: 質問文と選択肢だけの既知の関数操作を、全表示内容の照合後に許可。未知の形式や保護情報入りは停止を維持
- #113: 固定の`git remote`一覧表示をローカル操作として区別。送信・更新・動的命令へ許可を広げない
- #140: 人工試験で見つかったPython公開文字列送信の不要な停止を別Issue化。安全な解決方法が未実装なので保守的停止を維持

通常利用開始は研究全体の完成ではありません。固定の人工試験を、実モデルが試行錯誤する探索、任意命令の安全性、将来予測の有効性の証拠にはしません。

## 以前の完了地点: alpha.15

Codex対応、alpha.12の公開判定、実projectでの初回集計、最初の検出改善、実運用DBで見つかった時間切れ対策を`0.1.0-alpha.15`までに完了しました。以下はalpha.15時点の履歴であり、現在の公開・導入状態は冒頭を参照してください。

Issue #114では、番号を直接書いた単独の`gh issue view`を安全に読み取り操作と判断できるようにし、PR #118で取り込みました。Issue #119で`0.1.0-alpha.14`を公開し、現在のCodexへ導入しました。

その実確認中、3.4GBまで増えた保存DBが冷えた状態では操作前の判定に約12秒かかり、Codexの10秒制限で判定が打ち切られて操作が始まる問題を確認しました。Issue #122では次を実装しました。

- 現在の実行だけを読む索引を必ず使い、不要な全体走査を避ける
- 操作前の内部判定が7秒を超えた場合、途中結果を捨てて固定の停止結果を返す
- Codex側の操作前Hook制限を15秒にし、内部の停止結果を先に受け取れるようにする
- 必要な索引がないDBは、無理に検索せず安全側で停止する

PR #123を`main`へ取り込み、`v0.1.0-alpha.15`をGitHubの事前公開版として公開しました。PR #124で同じ内容を`public-alpha`へ反映しました。現在のCodexもalpha.15へ更新し、変更された操作前Hookを正規の確認画面から確認済みにしました。

新しいCodex実行で、設定済みのPromptPredictionでは次を確認しました。

- 番号を直接書いた`gh issue view 122 ...`は操作前判定を通過し、コマンドが開始した
- 変数を使った`n=122; gh issue view "$n" ...`は操作前に停止し、コマンドは始まらなかった
- 同じ実行番号で開始、操作前、操作後、終了の記録が保存された

未設定の新規Git projectでは、ToolUseProxyの案内を出さずにローカルコマンドが実行され、保存件数は0でした。rootの`protected_sources.json`は作業前後で同一です。

## 試験結果

- alpha.15実装時の全体試験: 1162件合格、2件スキップ、補助確認1596件合格
- 最終修正後の対象試験: 16件合格
- Python 3.11 / 3.12、macOS導入確認、再現可能な配布物確認: すべて合格
- 公開候補は`main`の取り込み結果と同一
- `public-alpha`は`v0.1.0-alpha.15`と同一内容
- 導入済みの監視処理とHook定義は公開版と同一

## 完了したIssue

- #99: 実project試行の設計と初回比較
- #101〜#106: 内容を保存しない評価、訂正、自動比較、改善提案Issueへの接続、実利用確認
- #107: 未設定projectでは通知・記録・停止を行わない
- #114: 固定番号の`gh issue view`で生じる不要な停止を減らす
- #119: alpha.14の公開・導入
- #122: 操作前判定の時間切れによる素通りを防ぐ

## 既存の未完了Issue

次は、初回比較で見つかった「外部への操作かどうかの判断」を、関数操作と端末操作に分けて改善します。

1. #144: 人工試験の中断予約・再開・受信設定エラーを厳密化する
2. #130〜#138 / #140: 自動探索、原因調査と比較、将来予測、人工試験で見つかった追加改善
3. #115: 観測できる範囲を確定できない問題を減らす
4. #116: 記録が不完全で原因を決められない問題を減らす
5. #36: 直接照合、送信内容の解決、情報のつながりを比較する
6. #54 / #24: 通信の観測と動的な端末命令の判断を改善する
7. #38: Git pushで実際に送られる内容を解決する
8. #46: ローカルの意味類似を観測専用で比較する
9. #37 / #39: タスク、別担当、Git境界での引継ぎを設計する
10. #55: 内容を保存しない監査と将来の学習評価を整備する

Issue #112と#113は別Goalで再現・実装・回帰試験を完了しました。公開・導入の状態は#139で別に管理します。

## 次のセッションで最初にすること

1. branch、`git status`、親Issue #153、Issue #159、Project #1、現在のGoalを確認する
2. [保存容量と自動整理](../設計/保存容量と自動整理.md)、[実装タスク](実装タスク.md)、[現行研究方針](../研究/現行研究方針.md)を読む
3. 完了済み実装を作り直さず、この文書冒頭の最新の実装状況から続ける。#188と#159の実機受入は最後に残す
4. 既存Issueを重複作成せず、各Issueを一つのGoalとして、実装、全体試験、PR統合、IssueとProjectの完了まで進める
5. #159は実動作確認の残件として保持する。初回整理を繰り返さず、Pluginを自動で有効化しない。再検証時はその時点の導入版でHook到達・保護対象の実行前停止・通常操作の復帰を確認する
6. 現在は#132。次に#140の修正と正常操作を含む比較を行い、自動探索・改善一式の確認後に#133〜#138へ進む

## 作業時の注意

- repository rootの`protected_sources.json`は利用者管理ファイルである。削除、編集、commitをしない
- 保護対象の内容、受け取った生データ、URL、接続先、絶対パスをIssueやCIの成果物へ出さない
- 意味類似だけの結果やAIの判断を自動許可へ使わない
- 判断不能な外部操作に保護対象由来の情報が届く場合は、安全側の停止を維持する
- Hookから見えない操作を保護済みと表現しない

## 新しいセッションへの開始文

> `/Users/mani/Developer/ToolUseProxy`の開発を引き継いでください。branch・git status・現在のGoal・Project #1と、この文書の最新の実装状況を確認してください。#132のIssue連携、#140の誤停止修正、#133〜#138を進め、人間による実機受入は最後に残してください。ToolUseProxyは無効のまま、root protected_sources.jsonは閲覧・編集・削除・stage・commitしないでください。

## #188 継続監査の追加（2026-09-18）

- `3f73fd2` の全体試験は独立checkoutで1499件＋補助1606件成功、2件skip。
  Ruff全体も成功。試験終了を確認して独立worktreeは削除済み。
- 管理者登録済みprojectのconfig set/unset、protect remove/reconcile/migrate applyを
  通常CLIで拒否し、管理者状態導入時のuninstall applyも拒否する。DB/保護リストの
  読取前に止める。通常CLIのrevision/tokenを人間の承認へ昇格させない。
- 管理者用入口は実装済みなので、Unsetupの日本語説明を「未実装」から
  「通常CLIに承認権限がなく、独立した導入済み管理者入口と人間操作が必要」へ修正。
  対象試験55件とRuff成功。直接ファイル編集やHook差替えは権限分離の未検証範囲。
- 管理者用単一ファイルをrelease candidateの4つ目のartifactへ追加。
  schema 2のmanifest/checksum/SBOMへ含め、wheel内の管理者ソースから再生成した
  byte列との一致をoffline検証する。旧schema 1候補の検証互換性も維持。
  独立checkoutで配布物/管理者入口21件＋補助5件、Ruff成功。
  候補生成・検証は権限昇格やMacへの導入を行わない。実機ゲートは未完了。
- `6fa1ff9` のclean独立checkoutから候補を実際に生成・検証済み。
  場所は `/private/tmp/tooluseproxy-188-rc-6fa1ff9`、artifact 4件、archive member 349件。
  管理者用ファイルSHA-256は
  `740d530e6a74519276b07b5d238dcec63cbef6e850907358ddb33179934343ec`。
  source用worktreeは試験終了後に削除済み。候補は管理者レビュー用に保持している。
- [実機受入手順](Unsetup実機受入.md)を追加。人間だけが操作できる管理者環境を
  用意できるか、利用者へ質問済み。回答待ち。現在のタスクのUIDは501で、
  管理者への昇格・導入・Plugin有効化はしていない。
- `6fa1ff9` CIのrelease candidate/macOS smokeは成功、Python 3.11/3.12全体は実行中。
  人間の管理者境界をエージェントが代行して「検証済み」にしない。
- 最新文書head `7e558ee` のPython 3.12 CIで120秒ちょうどの期限試験が失敗。
  実装head `6fa1ff9` のCIは全件成功していたが、浮動小数点の減算で経過120秒が
  119.99999999999999になる境界を再現できた。開始時刻8.003の人工時計で固定し、
  減算からdeadline自体との比較へ変更。期限120秒は維持。対象19件・Ruff成功。
  `/private/tmp/tooluseproxy-188-rc-6fa1ff9` は旧候補であり実機導入に使わない。
  修正版のcandidate生成・CI確認が次の作業。管理者環境の回答待ちは継続。

### #137 読取専用の入力版取得（2026-09-18）

- `research/flow_forecast/recording/source.py` に `EventSource` を追加。
  既存events.dbへmode=ro/query_onlyで接続し、単一transactionで対象workspace/sessionの
  event、operation/outcome、resource version、保護登録、runtime settingsの版をhash化する。
  保存済みpayloadはローカルでhash化し、snapshotの返却値へ含めない。
- 件数・byte数・SQL時間を制限し、上限超過を途中までの有効なsnapshotとして返さない。
  DB初期化・migration・保護対象ファイルの読取・実利用DBへの接続は行っていない。
- 人工DBで遅いI/O更新、他workspaceの分離、設定/保護登録更新、書込拒否、DB未作成、
  上限超過を確認。recording対象31件成功、Ruff/diff check成功。
- このsnapshotはモデル用Prefixではない。workerへの接続、管理者lease、CLI/表示は未完了。
  F04の実データへの適用可否と#204の独立holdout不足も未解決のまま。

### #137 単発CLIとローカル説明（2026-09-18）

- `python3 -m research.flow_forecast.recording.cli` を追加。人工データの
  init/enable/candidates/enqueue/run/history/disable/recoverを接続。
  workspaceはdatasetの場所から導出するsynthetic名前空間とし、実projectの設定を変更しない。
- 元のsealed datasetとモデルを再読込してworker前後の版を照合する。
  モデル不在・不正、元入力の取得不能は予測なしの診断としてjournalへ記録する。
  `history --format text` は日本語の履歴説明。未確定値を0にせず、操作許可とは扱わない。
- `inspect-input` はregistered workspaceの管理者lease内で読取専用snapshotを取得。
  実イベントをモデルへ渡さずout_of_domainと入力版だけを返す。
  人工のauthority storeで、処理中のdeactivating・例外時のlease解放・inactive後の
  入力読取拒否を確認。OS管理者との実機権限分離を確認した証拠ではない。
- 関連37件、全体1955件＋補助1609件成功（2件skip）。Ruff全体、diff check成功。
  Draft PR #206を作成。CIは検証中で、Issueをcloseする変更ではない。
  起動手順と残る受入は `research/flow_forecast/recording/README.md`。
  #137はまだ未完了。実入力の構造変換・F04有効性・実機負荷/既存保護の確認が残る。

### #137 入力更新の追加確認

- events.dbのsnapshotへ同じworkspace/sessionのsink_candidatesを含めた。
  eventのsequenceが増えない候補metadata更新、resourceのcontent_hash更新でも入力版が変わる。
- 実際の予測子プロセス終了時に元の人工datasetの封印を失わせ、予約済みPrefixは残っていても
  workerがinput_unavailableとして予測を破棄することを確認した。
- source/CLI対象11件成功、Ruff/diff check成功。関連試験合計39件。

### #138 判定部分の着手

- #137 PR #206の最新headは8f56a41。レビュー可能にし、CI run 35323600591を確認中。
- CI待ちの間に専用worktree `/private/tmp/tooluseproxy-issue-138`、branch
  `codex/issue-138-early-stop-comparison` でF06の判定部分を実装。
  #137 branchから分岐しており、PR #206統合後にmainとの関係を整理する。
- `research/flow_forecast/early_stop.py` は実験専用で既定無効。既存blockを解除しない。
  対象/入力/モデル/世代/期限の不一致、未解決・列挙外の確率が残る予測では追加停止しない。
  9件のunit試験とRuff成功。正例は人工fixtureでありモデル有効性の証拠ではない。
- 対照runner、独立した受信証拠、正常作業の完了率、比較レポートは次の実装。
  #137/#138とも実機受入や有効性の未確認を残してcloseしない。Pluginは無効のまま。

### #137 統合、#138 対照runner

- PR #206は8f56a41のCI全4件（run 35323600591）成功後にsquash統合済み。
  mainは505f415。#137は実機受入・F04依存を残すためopenを維持。
- #138に `early_stop_trial.py` と `early_stop_runner.py` を追加。
  既存判定を保存し、追加停止は別記録。停止後も独立受信側を照会し、
  正常完了率と保護情報の到達率を対になった条件で比較する。未知受信は未知のまま。
- 初回Docker接続確認 `/private/tmp/tooluseproxy-138-wiring-smoke-v1` は11試行完了。
  意図的な閾値0で追加停止2件、正常完了2/2→0/2、保護到達0/2→0/2、停止/受信矛盾0。
  採用根拠ではない。モデル固定検査とrunner実装digest追加前の接続確認として保持。
- 対象18件とRuff成功。予測中にjournalを無効化すると結果を破棄し追加停止しないが、
  既存blockは維持する。現行モデル固定・実装digest付きの実通信、全体試験、CIは次。
- 次は#138のbranchを統合済みmainへ載せ替え、残る検証とPRを進める。

### #138 現在の検証位置

- main 505f415へrebase済み。機能headはc084ac2。
- モデル固定後の `/private/tmp/tooluseproxy-138-comparison-v2` は閾値0.5で11試行完了。
  正常完了2/2→2/2、保護到達0/2→0/2、追加停止0、矛盾0。改善の証拠ではない。
  初回0閾値と今回0.5はそれぞれ新規出力先の有限11試行であり、既存batchを再送していない。
- 課題群数は「独立性を確認した群」ではないため、reportのフィールド名を
  declared_root_familiesへ修正。元のv2成果物は当時の出力として保持する。
- 全体pytestはexec session 35750で実行中。開始時のコードはc084ac2＋上記field名修正。
  終了を確認してから、対照試行のreceiver構造記録をresultsへ永続化する修正を行う。
  現状の到達結果/digestだけでは、Docker削除後に独立受信証拠を再監査できない。
- Project #1の#138をIn Progressへ更新。#137は実機受入待ちでopenを維持。

### #138 受信証拠を保存して再監査

- exec session 35750は終了0。受信記録保持追加前の全体1975件＋補助1609件成功、2件skip。
- ReceiverRecordをresults.jsonlに保存。操作ID・受信サイズ・保護情報の有無と観測状態を
  hash/整合性検査で照合し、Docker終了後に保存8行から同じ比較結果を再計算する。
  `early_stop_audit` は読取専用で再監査するCLI。旧v1/v2のdigestだけの記録は拒否する。
- 対象20件、Ruff、diff check成功。最新の有限Docker確認は
  `/private/tmp/tooluseproxy-138-comparison-v3`。閾値0.5、11試行、正常完了2/2→2/2、
  保護到達0/2→0/2、追加停止0、矛盾0。別CLIからの保存証拠再監査も成功。
  実装digest c8f2a494c34195c2ee4657925b6eaa57f625bfc73f0a8db51027e65878c1ee88。
- 流出減少や未使用課題への有効性は未実証。本番導入はしない。
  次は最新headのCI/レビューとPR統合の確認。#188/#137/#138の受入未完了を捏造しない。

### #138 PR #207のレビュー修正

- PR #207のbb4fb29はCI全4件成功。CodeRabbitの2件（実装provenanceの範囲不足、
  unknown到達と存在する受信記録の不整合）をコードで確認して修正。
- 自己点検で1試行をbaseline/forecastへ複製できることを失敗試験で再現し、
  attempt/step/tool_use IDの再利用と異なるrunの混在を拒否した。
- source-provenance.jsonへアプリのPythonソース一式のファイル別hashを保存し、
  実行前後で照合する。モデルartifactとDockerのcontext/image識別は別に保持する。
- 対象25件、Ruff、diff check成功。v3の保存8行は新検査で再集計成功。
  完全なsource provenance付きのDocker確認は次。旧v3をその証拠にしない。
- #188用の候補は /private/tmp/tooluseproxy-final-candidate-bb4fb29 に生成・別verify成功。
  4 artifact、352 archive member、manifest schema 2、未公開/未導入。
  /private/tmp/tooluseproxy-final-acceptance-bb4fb29.md に未完了の受入を整理。
  管理者環境を人間側で用意できるかasync質問済み、回答未確認。

### #138 レビュー修正後の実行証拠

- `/private/tmp/tooluseproxy-138-comparison-v4` は完全なsource provenance付きで11試行完了。
  閾値0.5、正常完了2/2→2/2、保護到達0/2→0/2、追加停止0、矛盾0。
  保存8行のoffline再監査成功。保存source provenanceと現在のPython実装一式も一致。
  実装digest 5c685be03abaea4ebebf9a4a638d74185234543f6c8ec42b257eee16771605c1。
- PR #207のCodeRabbit指摘2件に対応済み。最新headをpushし、CIを確認する。
  mainにはrequired status checksを設定したbranch protectionはないが、CIは全件確認してから統合する。
  レビュー待ちを理由に保護設定を変更したり、チェックを捏造したりしない。

### 2026-09-19 ログUI完了後のGoal再開（#204）

- 利用者はログUIをいったん完了とし、他機能のGoal再開を指示。UIのPR #211は
  CI全4件・レビュー修正を確認して統合済み（68ddf21）。UIの追加刷新は現在の主題ではない。
- active Goalは#204の独立課題/生成モデル証跡/未使用評価の収集・再評価。
  #204の後に#137/#138。#188の人間による実機/管理者境界受入は最後へ残し、他実装を止めない。
- PR #212は予測関連209 testsとCI全4件成功後に統合（0b4ce17）。train/calibration/testを
  個別保存し、prepareがtestを読まず、evaluateが台帳への開封commit後にtestを読む。
  再試行で未使用に戻さず、調整後退役を記録。旧データ変換は既読のままで受入を昇格しない。
- #212のCodeRabbitは利用上限でレビューなし。成功contextをレビュー完了と取り違えない。
  自己点検とテストは済ませた。保護設定やCI条件は変更していない。
- PR #213は生成実行証跡を採用planとtrialへ結び付ける。指定モデル・CLI版・call ID・
  prompt/events/proposal digest・取得できたusageを試験開始前に保存。F01取込auditにも継承し、
  予測器入力からは分離する。旧runはgeneration=null、resolved model版は未検証を維持。
  人工試験+取込290 tests、Ruff、diff check成功。最新headのCI/レビュー/統合確認は次。
- #213作業場所は/private/tmp/tooluseproxy-generator-evidence、branch
  codex/generator-execution-evidence。#212作業場所は/private/tmp/tooluseproxy-forecast-holdout。
  shellは/bin/zsh、login=falseを使う。
- 次は独立課題の由来/関連群の収集記録と実行経路への接続。520群の必要条件を名前/seed違いで
  水増ししない。実モデル証跡・全体費用・独立保管/未使用評価・有効性の受入はまだ未完了。
  #204/#137/#138/#188はopenのまま。Plugin無効、root protected_sources.jsonは触らない。

### 2026-09-19 #204の由来接続と評価容量

- PR #213はhead 20c95c5の全CI成功後に統合（61f667c）。生成実行証跡をtrialとF01 auditへ接続済み。
- PR #214はhead b7a82c7の全CI成功後に統合（52a518e）。課題catalog、元文書hash、root/design対応、
  関連群を既存人工artifactの収集へ接続。完成印と再読込照合あり。意味上の独立性は未証明。
  外部CodeRabbitは利用上限でレビューなし。成功contextをレビュー完了とは扱わない。
- 次の容量変更は/private/tmp/tooluseproxy-forecast-capacity、codex/forecast-collection-capacity。
  aggregate Datasetを20,000分岐、model rootを10,000まで扱い、重複prefixをsplit入力から除く。
  32MiB/研究300秒・1GiBと、実行バッチ20試行・1,800秒・1GiB・自動延長禁止は維持。
  baselineの集計とoptimizer中にもBudget確認を追加。
- 実装hash付き機械的fixtureの4,000 root/16,000分岐で保存・8方式評価が117.05秒、
  ピーク193,314,816 bytes、保存15,176,297 bytes。capacity.mdとresults/capacity-20260919.json参照。
  同じ設計としてcatalog収集すると1群へ結合されることも実確認。独立4,000課題とは数えない。
- 実課題の試行前にcatalogと生成実行を結び付ける収集runner、全model callの費用記録
  （終了/棄却/失敗を含む）、独立課題の実収集、未開封testとモデル版の実証、再評価は未完了。
  CLI版はこの時点で監査対象のcodex-cli 0.153.4と一致。Docker daemonは未起動だった。
  Dockerの起動準備は独立して進められるが、この確認では実試行/モデルAPI呼出はしていない。
  Pluginは無効のまま、root protected_sources.jsonと実利用DBは触らない。

### 2026-09-19 全生成呼出の記録と実CLI確認

- PR #215は11ac196の全CI成功後に統合済み。評価容量と学習中Budget確認を接続した。
- PR #216は/private/tmp/tooluseproxy-call-accounting、codex/generation-call-accounting。
  全callを事前pending記録し、終了/拒否/重複/失敗を含めてusage既知/未知を集計する。
  人工試験+取込296 tests、Ruff、diff check成功。CI/レビュー/統合確認は次。
- Dockerを起動し、commit636d995でgpt-5.5指定の有限1バッチを実行した。
  出力/private/tmp/tooluseproxy-204-call-costs-v1、15試行/180秒/model_calls=2。
  対照全項目成功、公開人工データ試験1件完了、2回目生成がinvalid_model_proposalで終了。
  controllerプロセスは終了済み。自動再試行/上限延長なし。DBのrunningは再開可能な失敗記録。
- 2call中1callのusageは既知（input3703/cached0/output114）、もう1callは不明。
  閉じた証跡はresults/generation-costs-20260919.json。これを独立課題の実績にしない。
- 実確認で次の不足が確定：CodexProviderはproposal検証失敗時に、CLIが返していた可能性のある
  usageも失う。次は提案採用の検証とexecution/usage receiptの抽出を分離する。
  過去の未取得usageを捏造せず、新しい確認は別の明示的有限バッチとして記録する。
- 試行前catalog割当、独立した新課題/未開封test、モデル版の実証、料金証拠、再評価は残る。
  Goalと#204は継続。Plugin無効、root利用者管理ファイル/実利用DBには触れない。

### 2026-09-19 提案が無効でもusageを保存する修正

- PR #216でexecution receiptと採用proposalのgenerationを分離した。失敗でも正常な
  turn.completed usageがあれば保存し、無効な提案は実行しない。303 tests/Ruff成功。
- head fa8bb5aの別有限バッチ/private/tmp/tooluseproxy-204-call-costs-v2では対照全項目成功。
  1回目の提案がinvalid_model_proposalとなり試験0件、input3707/cached0/output272 tokensを保存。
  token_totals_complete=true。v1の不明なusageは変更していない。controllerは終了済み。
- 1call/45秒/試行0件の追加形式診断は正常なJSON提案1件で、エラーは再現しなかった。
  入力3703/cached2560/output111。生の文章は保存せず、診断・usageの閉じた証拠だけ保持。
  results/generation-costs-v2-20260919.jsonとproposal-shape-20260919.jsonを参照。
- 今後はJSON/提案schema/メッセージ数/完了数/イベント形式を閉じたvalidation_stageに記録する。
  推測で応答の検証を緩めてはいない。モデル版・価格・独立課題の受入は依然未実証。
- 次はPR #216最新headのCI確認・統合と、catalogを試行開始前に割り当てる収集経路。
  既読artifactをまとめるだけで未使用testにしない。人間の#188受入を理由に実装を止めない。

### 2026-09-19 試行前の課題割当と収集の接続

- PR #216は全CI成功後にmain b9c086aへ統合済み。
- PR #217は/private/tmp/tooluseproxy-task-assignment、codex/pretrial-task-assignment。
  catalog・由来文書・選択課題を封印し、モデル呼出前にidentityへ保存する。
  再開での追加/削除/変更、割当なしの旧runの昇格、取込時の課題指定し直しを拒否する。
  flow-lab/flow-forecast 531 testsとRuff成功。CI/レビュー確認後に統合する。
- de7e3aで15試行/180秒/2モデル呼出の有限バッチを実行。
  /private/tmp/tooluseproxy-204-assigned-search-v1で対照全項目成功、公開人工HTTP1件完了。
  input7983/cached0/output180 tokensを2callとも保存。自動延長はしていない。
- /private/tmp/tooluseproxy-204-assigned-collection-v1へ保存割当から収集しread_collectionで再検証。
  results/pretrial-assignment-20260919.jsonに閉じた証跡を保存した。既存課題と同じ1群なので
  独立新課題数0、未使用test=false。モデル版/価格/任意課題の成功判定の証明にはしない。
- #204の次の残件は独立した課題の実行可能な成功条件と対照分岐、未開封評価用の収集。
  HTTP文脈の割当だけでは任意課題のoracleにはならず、adaptive履歴を固定分布と扱わない。
  Goal/#204は継続。Pluginは無効、root protected_sources.json/実利用DBには触れない。

### 2026-09-19 モデルの完了申告と課題達成の分離

- PR #217はaea797fの全CI成功後にmain d710ab4へ統合済み。外部レビュー結果は未取得。
- PR #218は/private/tmp/tooluseproxy-task-completion、codex/synthetic-task-completionで実装。
  assignment schema=2は機械的completion条件を生成前に封印。schema=1は未定義のまま保持。
  公開人工HTTPの送信件数(1〜10)/encodingだけを受信観測から判定し、モデルのcompleteと分離。
  不足/超過/形式違い/保護対象送信は達成にせず、欠測/受信不明はunknown。任意課題oracleではない。
  結果、再開、F01取込へ条件/割当hash/観測hashを引き継ぐ。関連547 tests成功後CLI試験1件を追加し成功。
- source0f901d8で15試行/180秒/2callの有限人工バッチを実行。対照全成功、
  公開データ1件送信という封印条件を達成。モデル呼出2回input8025/cached2560/output138を保存。
  /private/tmp/tooluseproxy-204-completion-search-v1、同completion-collection-v1で
  割当→実行→達成判定→収集→再読込の一致を確認。results/task-completion-20260919.json参照。
- 接続確認用の既存課題1群であり、独立新課題数0/unused_holdout=false。
  PR統合確認、一般の実行可能な課題設計、対照分岐の収集、独立holdout/モデル版の実証は残る。
  Goal/#204継続、Plugin無効、root利用者管理ファイル/実利用DBは触れない。

### 2026-09-19 同じ生成計画のobserve/enforce対照収集

- PR #218の外部指摘2点を7ab3a69で修正。完了済みrunも実装版が違えば再判定を拒否し、
  形式違いを成功件数に含めない。判定器hashを返し、F01取込は元の結果の再掲ではなく
  現在の判定器での導出と明記する。31関連tests/Ruffと全CI成功、main3224d72へ統合済み。
  外部指摘2件は解決。修正後の再レビューはrate limitで未実施。
- PR #219は/private/tmp/tooluseproxy-paired-trials、codex/paired-assigned-trialsで対照収集を実装。
  完了済み割当runから計画を実行前に固定し、受信環境を分けてobserve/enforceへ流す。
  元の生成証跡・課題群を保持し、F01 datasetへ同じprefix/branch/groupで保存する。
  558 tests/Ruff成功。計画最大7操作、各条件3対照、全体20操作/8試行・1800秒・1GiB以下。
  各操作前のdeadline確認に加え既存Dockerコマンドtimeoutを利用。途中失敗の自動再実行はしない。
- 7f69fceで既存completion-search-v1の公開送信計画を180秒の有限バッチで実確認。
  /private/tmp/tooluseproxy-204-paired-collection-v1、全対照成功、両条件で課題達成、
  8操作/8試行、追加モデル呼出0、F01の2分岐を再読込検証済み。
  results/paired-collection-20260919.jsonに閉じた証拠を保存。PR化/レビュー/CI/統合は次。
- 元計画はadaptive探索由来であり確率はnull、中間truthはunknown。
  独立新課題数0・unused_holdout=falseのまま。独立課題と生成モデル版の実証、未使用評価は未完了。
  Goal/#204継続。Pluginは無効、root利用者管理ファイル/実利用DBは触れない。

### 2026-09-19 独立群の改名対策と外部設計元候補

- PR #219は28bcb2cの全CI成功後main d768a0aへ統合済み。外部レビューはrate limitで未実施。
- /private/tmp/tooluseproxy-task-graph-groups、codex/task-graph-groupingで
  flowノード名を一括改名すると群が増える問題を再現し修正。道具・操作ラベルを残し、
  ノード名に依存しない有向グラフの特徴で保守的に結合する。完全な同型/意味の判定ではない。
  新しい証拠にgrouping_method v2を記録し、旧分割は新規収集として明示的に再構成する。
  566関連tests/Ruff成功。100操作の改名fixture1000件が1群、約4.4秒。独立試料ではない。
- 外部設計元を公式資料で確認。APIFlow-Benchは467課題/13worldの派生であり、467独立群と数えない。
  TaskBenchの固定commit7624cf3からDailyLifeのdata/tool_descとLICENSEのみ取得しGit blob SHA照合。
  ローカル/private/tmp/tooluseproxy-taskbench-source-audit-v1。データ原文/第三者コードはrepoへ追加しない。
- audit_taskbench CLIは読取専用で式や道具を実行しない。結果はresults/taskbench-source-audit-20260919.json。
  4318件中、40道具の一覧と整合する4050件/2854構造特徴。252件道具一覧外、8件未知接続先、
  4件ノード重複、4件読取不成立を除外。固定版と監査/群分けコードhashを保存し再現一致を確認。
  accepted_f02_roots=0、independence_verified/prior_nonuse_verified=false。
- 次は独立候補を実行可能な人工API課題へ接続する。宣言上の依存辺を情報流truthと誤認せず、
  人工source/receiver・実行I/O・成功条件を固定し観測する。現行HTTP DSLへ名前だけ置換しない。
  公開資料を読んだこの設計調査は未使用holdoutではない。source-candidates.mdを参照。
- 生成モデルの実解決版は未実証。OPENAI_API_KEY/ANTHROPIC_API_KEYの存在だけ確認したが未設定。
  値を出力/保存していない。Codex既存認証によるrequested model/CLI版をprovider証明へ昇格させない。
  Goal/#204継続、Project #1はIn Progress。Plugin無効、root利用者管理ファイル/実利用DBは触れない。

### 2026-09-19 生成モデル証跡を凍結済み比較へ接続

- PR #220はb584095の全CI成功後main2515811へ統合済み。外部レビューはrate limitで未実施。
- /private/tmp/tooluseproxy-generator-evaluation、codex/generator-evaluation-evidenceで
  compare/compare_holdoutへ--collectionを追加。prepareは収集sealのhashだけを固定し、
  holdout開封予約後に完全な証跡を読む。対応Dataset、run/branch、生成計画hash、
  requested alias、receipt重複を照合する。証跡変更/指定削除を拒否し、失敗した開封も消費する。
- 各予測方式の条件別に指定aliasの確率/経路/警告性能を集計。全体の凍結閾値を共用し、
  関連群内のalias混在/欠測で群を分割しない。actual model versionは未検証のため
  evaluated/resolved_model_versions_verified=false、F02モデル一般化条件を合格にしない。
- 577関連tests成功後、追加の混在receipt回帰試験を含む12件成功、Ruff/diff check成功。
  source69fe8feで保存済みassigned/completion collectionの実生成receipt各1件を検証。
  requested gpt-5.5へ対応、両収集物とも固定分布の訓練群がなく比較拒否を確認。
  results/generator-strata-20260919.json参照。追加モデル呼出/試行0、独立F02新課題0。
- この修正はモデル別レポート経路の実装。独立課題/固定分布/未使用評価/実モデル版は未完了。
  人間へ利用可能な固定モデル実行先の種類を質問済み、未回答。認証値は求めず、独立実装を継続する。
  PR化/レビュー/CI/統合を継続し、Goal/#204はactiveのまま。Plugin無効、実利用DBは触れない。

### 2026-09-19 中間の入出力を実観測する収集器

- PR #221はc878ef9の全CI成功後main0ba5dc0へ統合済み。外部レビューはrate limitで未実施。
- /private/tmp/tooluseproxy-stateful-capture、codex/stateful-synthetic-captureで
  stateful_collection CLIと閉じたStatefulTransportを追加。同一隔離sender内で
  read/copy/encode/save/sendを実行し、別プロセスが実ファイルの一致/復号一致を観測、
  receiverのbody hashとも照合する。各操作前に実装・plan・試行予約を記録し、途中失敗は再開しない。
  最大7操作×2条件+6対照=20試行、各明示バッチ1800秒/1GiB以下。任意コード/パス/送信先を受けない。
- 生成前に固定した単一決定的人工シナリオだけを確率1でF01化。adaptive資料は変換しない。
  実際に共通して観測したprefixだけを使用、途中のhash鎖/拒否receiptを再検証。
  拒否された操作を実行済みとして記録せず、未実行の先はcensoredのままにする。
- 590関連tests/Ruff成功。有限Dockerバッチ5回（各上限14試行/180秒、各実11試行）を実施。
  最終capture版2fb50f5、/private/tmp/tooluseproxy-204-stateful-public-v3と同protected-v2。
  両者のobserveは4操作完了し、未知辺0で公開no/保護yesの到達を独立受信とbyte観測で確認。
  enforceは両者とも最初の操作で拒否。publicの正常完了改善は未実証。
- 当初はローカル処理にもHTTP importを付けてexternal_static_classifiedとなったため除去。
  最終版でもexternal_unknown_pendingで拒否される。理由は保護対象のある環境でこのPython形式の
  payload確認ができないこと。検出器の保護を緩めたり、収集器だけを許可する特例は追加していない。
  各操作のHook受領・判断理由を保存する。policy traceは同じsession内の累積と明記する。
- 2ed9f71の再取込検証で最終2実行のF01一致を確認。results/stateful-capture-20260919.jsonに
  全5バッチ55試行、保存量、出典・結果を保持。正確な経過秒は旧reportに未記録のためnull。
  追加生成モデル呼出0。最終2実行は同じbuild contextでもimage IDが変わり、生のsnapshotでは2群。
  同じpipelineの公開/保護対照である明示的なrelated_rootsを追加した再集計では1群。
  元の保存済み分割は変更せず、両値と関連の根拠を結果に記録。独立新課題0/未使用holdout=false。
- 自己レビューで、操作予約にstep ID/command hashを追加し、Hook受領・判断をdispatch前に
  別artifactへ保存するよう補強。途中失敗でも対応を失わず、再実行はしない。
  今後の完了reportは実測経過秒とreport書込前の保存容量も記録する。関連12 tests/Ruff成功。
  上の実Docker結果に未記録だった経過秒を遡って補完したものではない。
- 次は新しい課題の事前割当・実生成計画をこの実観測経路へ接続する。TaskBenchのAPIを
  名前だけfile/httpへ置き換えず、課題の実装意味/成功条件を観測できる形で設計する。
  独立課題数・実モデル版・F02の有効性は未達。Goal/#204 active、Plugin無効を維持。

### 2026-09-19 事前の課題割当・生成1回からstateful収集へ

- PR #222の外部レビュー2件を920db19で修正。完全なHook receipt validatorを実行/取込で共用し、
  field集合、scope、trace構造・件数・長さ、reason上限、trace hashを再検証する。
  23関連tests、保存済みDocker2件の再取込一致、全CI成功。レビュー2件は解決確認済み。
  修正後の全体再レビューはrate limitのため未実施。
- /private/tmp/tooluseproxy-generated-stateful、codex/generated-stateful-assignmentで
  generated_stateful prepare/collectを追加。生成前に課題/成功条件/変換仕様/call予約を保存し、
  feedbackなしの1提案だけを受け取る。提案・生成receipt・prompt hash・課題を照合し最後に封印。
  不正/拒否/複数action/未対応表現/timeout/中断も費用記録を残し、自動再提案しない。
- literal/urllibの単一actionだけを、事前に示したread→encode任意→save→sendへ変換。
  これらのfile操作をモデルが自発的に計画したとは扱わない。stateful開始前intentへ生成証跡を結び、
  収集時は追加model callなし。public_deliveryは受信数とencodingから条件ごとに判定する。
- 実Codex gpt-5.5の1呼出をsource9e25ed2で実施。prepare-v1はinput4053/cached0/output56、6044ms。
  /private/tmp/tooluseproxy-204-generated-stateful-prepare-v1。費用単価・実解決model版は未確認のまま。
  source48360b3で同じ保存済み計画を10試行/180秒上限でDocker収集し、実測13.05秒、
  report書込前128012bytes。/private/tmp/tooluseproxy-204-generated-stateful-capture-v1。
  observeは公開送信の事前条件達成、enforceは拒否されnot_achieved。
- 課題割当→実生成receipt→実行intent→実観測→F01再読込一致を確認。
  results/generated-stateful-20260919.json参照。関連615 tests成功後、hash型の追加回帰を含む15件成功。
  Ruff/diff check成功。生成sourceと実行sourceを別に記録し、古い実行を現版で実行したとはしない。
- 次はstateful captureをtask_catalogの収集・generator_strataの比較へ読む入口を接続する。
  独立新課題0、unused_holdout=false、resolved model未検証。既知課題の実接続確認である。
  この実装で#204を閉じず、Goal active、Plugin無効、実利用DB/root利用者管理ファイル不使用を維持。

## 2026-09-19 保存済み生成パイプラインの取込

- PR #223 は main c99b7c6 へ統合済み。CI 35415593592 は全成功、CodeRabbit は rate limit。
- stateful_import が保存済みの生成記録・実測 I/O・Hook receipt・対照試行・reservation・F01 の対応を再検証する。
- task_catalog --stateful-captures から収集し generator_strata へ要求モデル名を接続。関連課題は同一群、生成 receipt 重複は拒否。
- 実測済み /private/tmp/tooluseproxy-204-generated-stateful-capture-v1 を /private/tmp/tooluseproxy-204-stateful-import-collection-v1 へ取込。train 1群 / 2枝 / receipt 1件 / gpt-5.5、追加呼出・trial 0。関連628テスト成功。
- #204 は継続。独立課題と calibration/test の必要数、実提供モデル版、未使用性・有効性は未達。既存の HTTP 課題を名前変更して増やさず、次は独立課題の意味と成功 oracle を備えた安全な人工環境の設計・実装へ進む。

## 2026-09-19 意味の異なる人工課題の実行

- PR #224 は main e27d3f2 に統合。CI 35416064899 全成功。CodeRabbit は rate limit（実レビュー完了とは扱わない）。
- 新規 task_worlds / task_world_collection は在庫引当・会議調整・明細集計の閉じた入力と実アルゴリズム、別定義の正解を持つ。既存隔離Dockerで load / compute / save / send を実行し、各操作をHook判定・予約・実測する。
- 実行元 dd47854。明示的3バッチ、各最大20試行/180秒/1GiB、実際は各11試行。/private/tmp/tooluseproxy-204-world-{inventory-public,calendar-public,ledger-private}-v1。追加モデル呼出0。
- 前2件はobserveで正常完了・保護受信なし。ledger privateは人工マーカー受信を確認。enforceはすべて最初の操作で停止。詳細 research/flow_forecast/task-worlds.md / results/task-worlds-20260919.json。
- 関連648テスト成功、その後追加の遮断/中断テストを含む新規22テスト成功。独立課題受入0。semantic / selection truthはunknown、F01取込・生成モデル接続・520群はまだ未達。次は意味的変換の有限介入検証と課題由来/生成実行への接続。Goal #204 継続、#137/#138、その後に#188の人間による実機受入。

## 2026-09-19 人工課題の評価接続

- PR #225 は main 740b92d へ統合済み。CI 35416497753 全成功、CodeRabbit は rate limit。
- task_world_import / task_catalog --task-worlds で保存済みの閉じた課題を検証・F01化する。compute と3課題種別を追加。semantic / selection は unknown、遮断後の未実行操作は作らない。
- 既存の3バッチを /private/tmp/tooluseproxy-204-task-world-collection-v2 へ取込。source 62d2cd55fa9516fb5ea05cc5e0c7fc5db3da83b3 で再構成一致・compare prepare/evaluate(train)確認。最終report /private/tmp/tooluseproxy-204-task-world-comparison-train-v3.json。3群6枝、正解unknown3、生成証跡0、inconclusive_do_not_adopt。
- v1 は課題種別unknown、v2は各課題種別付き。古い成果物は変更せず保持。追加モデル呼出/試行0。関連660テスト成功、課題種別追加後は関連33テストと統合後20テスト成功。
- 次: compute / selection の真値を検証できる閉じた証明と、生成エージェントの課題選択・実行との接続。独立群520などF02要件は未達、#204継続。ToolUseProxy無効、本番起動とroot protected_sources.jsonへの操作はしない。

## 2026-09-19 有限介入とCI監査の修正

- PR #226 初回CI 35416924977 のPython3.11は公開ソース監査で失敗。openai_key正規表現が task-world-comparison... の単語途中の接尾辞に誤一致していた。1b3ee91で英数字直後を除外し、引用/代入/Bearer/URL/区切りにある本来の形式は検出する回帰テスト追加。28テスト+4subtestsと履歴を含む公開監査成功。新CI 35417006972を確認する（前の失敗runを再起動しない）。
- 新task_world_interventionsで在庫数/注文順、所要時間/空き時間、返金/取消状態を各2種類変更。baseline込み各3試行をnetwork=noneのDockerで実行。source bf2507af4d7a977f5bd87832d7411fc21475c151、最大180秒/20試行/1GiBの明示的3バッチ、実際計9試行・全正解一致・介入6件すべて出力変化。
- /private/tmp/tooluseproxy-204-{inventory,calendar,ledger}-interventions-v1。追加モデル呼出0、独立課題受入0、semantic truthは昇格しない。関連665テスト成功、コンテナ名の事前記録/異常後片付け追加後7テスト成功。
- 有限介入は全入力の情報流証明ではない。次は固定コードの入力参照制約の検証と、生成エージェントの実課題実行への接続を進める。#204のGoal・F02条件は維持。

## 2026-09-19 介入証跡と元の計算の照合

- PR #226 は main 77f93e0 に統合済み。修正後CI 35417006972は全成功。CodeRabbitはrate limit。
- PR #227 に intervention_evidence を追加。intent/definition/cases/implementation/execution/reservation/script/result/report/予算を照合し、task_world実測の同じ課題・Docker context・実computeコマンドhash・baseline出力へ接続。
- 実測3課題すべて照合成功。results/intervention-bindings-20260919.jsonにvalidator source hashと対応を保存。追加試行/モデル呼出0。旧介入資料のコンテナID未保存は区別する。新規12テスト成功。
- F02の未知関係を既知には変えていない。有限入力依存の証拠が揃ったため、次は固定された計算コードの参照可能入力を限定・検証する契約と、生成モデルによる実行を接続する。

## 2026-09-19 固定計算の入力契約と正解経路

- PR #227 はmain f90a654へ統合済み。CI 35417436660全成功、CodeRabbitはrate limit。
- computation_contractが限定Python ASTの入力参照/使用前定義を検査。外部値sourceのみ、JSONコンテナと限定組込み以外を拒否。任意Pythonのsandboxやフィールド因果の一般証明ではない。
- checked_task_world / task_catalog --checked-task-worldsは、同じ課題/介入/実compute+localコマンドhash/観測bytesを揃えた場合だけclosed_computeとjson_projection証拠を使う。一般semantic/selectionと遮断後の未来は従来どおり未知。関連715テスト成功。
- source baa060b379b0ebe5ffa7580f3097ba5e6ab29417。/private/tmp/tooluseproxy-204-checked-world-collection-v1、plan-v1 / comparison-v1。observe/4 正常2/正例1/不明0、enforce不明3。全3群train、開発診断のみ、総合inconclusive_do_not_adopt。追加trial/モデル呼出0、独立課題受入0。
- 詳細research/flow_forecast/closed-computation.mdとresults/checked-task-worlds-20260919.json。次は実生成エージェントの課題計画/実行への接続と独立課題の拡張。#204のGoalと凍結F02を維持し、#137/#138、人間受入#188は指定順で続ける。

## 2026-09-19 実モデルの人工課題計画と実行

- PR #228はmain 8d0261aへ統合。CI 35417932021全成功、CodeRabbit checkはcompleted（inline指摘なし）。
- WorldPlanProviderは固定ツールの順序/出力範囲をモデルに提案させ、通常のCodexProviderのCLI版固定・host tools無効化・tool event拒否を再利用。計算コードはcontroller提供で、モデル実装とは主張しない。
- generated_world_plan prepareは最大1call/60秒、予約→応答/費用→封印。collectは課題/方針/receiptを試行前intentへ結び付ける。task_world_importとgenerator_strataを接続、生成receiptの使い回しは拒否。関連732テスト成功、その後strata接続を含む32テスト成功。
- 実測source22fb119: /private/tmp/tooluseproxy-204-generated-world-prepare-v1。要求gpt-5.5、入力3765/cached0/出力78、6250ms、load/compute/save/send・public。実版/単価不明。
- 同source capture-v1: 11試行/14.05秒/110035bytes。observe正常完了、enforce最初の操作で遮断。新規呼出は準備時1回のみ。
- checked collection-v1 + comparison-v1に接続。train1群/receipt1、要求alias層別scored、inconclusive_do_not_adopt。詳細generated-world-plan.md / results/generated-world-plan-20260919.json。
- 次は独立課題の設計・拡張と実生成エージェントの試行を増やし、F02の必要数・未使用課題/道具/モデル評価を満たす。既存課題の再実行を独立と数えない。#204は未完了、#137/#138、管理者受入#188は後続。

## 2026-09-19 追加3課題と6群の開発診断

- PR #229はmain 0f16bd4へ統合済み。CI 35418511814全成功、CodeRabbitはrate limit。
- routing（最小費用の辺緩和）、revisions（最大版/削除印）、prerequisites（全前提条件）の3設計を追加。元の3課題と限定AST契約は維持。CLI候補を登録表から取得。関連756テスト成功。
- source bdbf390。各介入3試行+実行11試行、6つの明示的バッチ、計42試行。各最大180秒/20試行/1GiB。/private/tmp/tooluseproxy-204-{routing,revisions,prerequisites}-interventions-v1 と world-{routing-public,revisions-private,prerequisites-public}-v1。
- routingは実モデル1callを封印して使用。routing-world-prepare-v1、要求gpt-5.5、入力3770/cached0/出力64、6055ms。observe正常、revisions privateは正例、prerequisites正常。enforceは全て最初で停止。
- six-world-collection-v1 / six-world-comparison-v1: 6宣言群12枝、全train、正常4/正例2、生成receipt2/欠損4、inconclusive_do_not_adopt。独立課題受入0・未使用評価なし。詳細additional-worlds.md / results/additional-worlds-20260919.json。
- #204継続。F02の独立520群、未使用区分、実提供モデル版、道具/課題/モデル一般化は未達。名前変更やseed違いを追加独立群にはしない。

## 2026-09-19 収集経路の実測費用と予約数

- PR #230はmain 8006424へ統合済み。CI 35418965877全成功、CodeRabbitはrate limit（外部レビュー完了とは扱わない）。
- measured_budgetはcapture/interventionsの既存readerとbind_captureを経由して実測小計を作成。二重capture拒否、共有介入は一度、読み取り間のhash不一致拒否。
- 実6課題/12完了バッチ:84試行、81.70秒、report前854011bytes。モデル準備・過去失敗・全保存量は含まない。全enforceが早期遮断なので、この時間を将来の正常完了へ外挿しない。
- 現在の経路は収集最大14+介入3試行/課題、2明示バッチ。独立520群が全て初回成功する条件でも1040バッチ/8840試行予約/520モデルcall。各180秒のdeadline合計52時間は実行時間予測ではない。provider費用・総保存量・end-to-end時間はnull。
- collection-plan.md / results/measured-budget-20260919.json参照。新規試行0、#204未完了。未使用課題/道具/モデル評価と独立群数不足を解消する作業を継続する。

## 2026-09-19 別モデルでの同一課題試行

- PR #231はmain 9816d03へ統合。CI 35419483830全成功。CodeRabbitはrate limitで外部レビュー済みとは扱わない。

- source d74bb41で在庫課題を要求gpt-5.6-terraにより1call生成。terra-prepare-v1:入力5844/cached0/出力61、5990ms。実提供版/単価不明。
- terra-capture-v1:11試行/13.834秒、observe正常/enforce先頭遮断。最大20/180秒/1GiB、自動延長なし。既存在庫介入へ結合。
- gpt-5.5在庫captureとtwo-model-collection-v1に封印しても1群。新規独立0。mixed群の表示からaliasが消える問題を61ecd7eで修正し、観測alias一覧を追加。学習混在群で既知のモデルをunseen testと表示しない。
- two-model-plan-v1 / two-model-comparison-v1はtrain診断、inconclusive。second-generator.md / results/second-generator-20260919.json参照。#204は未完了。

## 2026-09-19 APIモデル情報の取得経路

- PR #232はmain 2dfda80へ統合。CI 35419911082全成功、CodeRabbitはrate limit。
- generated_world_plan prepareに--provider openai-apiを追加。固定公式HTTPSへ1call、親プロセスdeadline最大60秒、256KiB応答、閉じた人工課題/JSON schema、toolsなし、retry/redirectなし。
- API receipt schema2は提供側reported_model・request/response ID hashを保持し、CLI証跡と区別。準備→試行→import→generator_strataのprovider_reported_models_by_partitionへ接続。実行receiptと採用receiptのAPI情報も照合する。
- 固定snapshot版の保証には自動昇格しない。APIキー未設定、実API呼出0。関連779テスト成功、その後API異常系と実子プロセスdeadline検証を含む17テスト成功。実API成功とは区別。詳細api-generation.md。#204の独立群・未使用評価・実モデル版確認は未完了。

## 2026-09-19 公開課題ソースの追加調査

- PR #233はmain 2e80f14へ統合。CI 35420738845全成功、CodeRabbit実レビューは対応指摘なし（対象head45a1c44を確認）。
- TaskBenchの依存リンクには単なる実行順序が含まれ、40tool定義にoutput schemaもない。これをF01の到達正解として直接importしない。
- API-Bankをcommit188835d4へ固定し9ファイルをhash照合して静的確認。予定APIは状態更新/所有者限定照会を実装する一方、SendEmailはsuccessを返すだけで受信を観測しない。上流ToolManagerの一括動的importは実行していない。
- apibank-source-audit.mdとresults/apibank-source-audit-20260919.jsonに候補範囲と次の有限adapter単位を記載。API/対話/domain数は独立群数にしない。実試行0/採用独立群0。
- APIキーは直近確認でも未設定。#204は独立holdoutと実モデル版の検証が未完了。API待ちの間、人工予定APIの状態とI/Oを実際に観測するadapterを次候補とする。元の保護境界・Plugin無効を維持。

## 2026-09-19 人工予定APIの有限実行

- PR #234はmain 8e816ecへ統合。CI 35421424941全成功、CodeRabbitはrate limit。
- 独自AgendaAPIのadd/get/publicと所有者別状態を実装。固定7caseの前後snapshot/戻り値を別宣言の期待値と照合。API-Bankコード/実利用DBは使わず、第三者コードを実行していない。
- agenda-api-v1/source d46774dは評価モジュールがimage対象外のためimport失敗。予約3、確認済みAPI結果0、elapsed不明。失敗を保存し、以後failure.jsonを記録するよう修正。
- agenda-api-v2/source dcc3123は10呼び出しすべて一致、2.5878秒/report前49283bytes。network none・非特権・read-onlyで実行。各最大20/180秒/1GiB、失敗を含む合計予約13、model0。
- 保存済み観測/予約hash再監査済み。agenda-api.md / results/agenda-api-20260919.json。API単体でありguard/receiver/F01未接続、独立追加0。次は実dispatch・I/O・stateを保ったguard/receiver収集接続。#204のGoal継続。

## 2026-09-19 予定APIのguard/receiver接続

- AgendaTransportは固定MCP JSON名/引数を実PreToolUseへ渡し、同じJSONをAgendaAPIへdispatch。senderの状態/戻り値を別observerで確認し、sendは実照会結果、別receiverでbody hash/サイズ/canary照合。
- source 8bc8e9cでpublic/private各1バッチ最大20/180秒/1GiB、各12試行。publicはobserve/enforceともallow・到着。privateはobserveでdeny後の人工送信が到着、enforceはsend前deny・未dispatch・到着なし。計24試行/model0/独立追加0。
- guard理由は保守的な拒否。exact情報追跡の成功扱いにしない。native host/PostToolUse/F01は未接続。制御試験は既存shell経路。Agendaは1関連family。
- 初回実測はreceiver IP未保存でsend script hashの事後再生成不可。現コードは今後の予約へreceiver_addressも保存。実測結果を後付けで補完していない。
- agenda-collection.md/results/agenda-collection-20260919.json参照。次は証拠reader/F01接続。#204のGoalは継続、元のPlugin/保護境界は変更なし。

## 2026-09-19 予定API記録のF01取込

- PR #235/#236はmain b1e3f94/9a6b133へ統合。CI 35422088940/35422439494全成功。CodeRabbitは双方rate limitで実レビューなし。
- agenda_import/read_captureとtask_catalog --agenda-capturesを追加。保存された宛先と呼出からscript hash再構成、実API/transport source・予約・guard・state・受信を照合。余分な試行や失敗記録、宛先欠損の旧形式は拒否。
- 7663b8cで公有/保護各12試行、計24（guard接続後累積48）を実行。各20/180秒/1GiB、model0。15.4442秒/12.2705秒、結果は前回と同様。
- 1関連群/2roots/6prefix/12枝へ封印、全train・真値unknown。compare train診断はinconclusive_do_not_adopt。モデル証跡0・独立追加0。agenda-import.md/results/agenda-import-20260919.json参照。
- 次は閉じたAPIの状態更新・フィールド選択の依存関係を検証する契約。独立holdout/モデル版不足は未解消。#204 Goal継続、Plugin/元保護境界は変更なし。

## 2026-09-19 予定APIのフィールド介入

- PR #237はmain 3f42130へ統合。CI 35423076693全成功、CodeRabbit rate limitで実レビューなし。
- agenda_batch --suite projection、5case×3呼出を追加。A private/B private/title/timeを個別変更し、全状態と公有/全体照会出力を別宣言の期待値と照合。API source固定hashが変わると拒否。
- source 9fbebc5で15試行/4.6995秒/57466bytes、model0。すべて一致。read_interventionsが15予約/全観測/container/各hashを検証し、bind_captureが既存公有・保護各2条件の照会結果へ結び付ける。
- build context/API sourceは同じだが実測image IDsは異なる。以前のimageの一つは現Dockerで見つからずlayer同一性は未確認。bindingに全ID/identical_image_verified=falseを保存。原因を推測せず、過去記録を変更しない。
- agenda-projection.md/results/agenda-projection-20260919.json。有限介入だけで非干渉性を証明しない。F01 selection/unknown維持・独立追加0。次は閉じた入力domain/実装限定の依存関係契約。#204 Goal継続。

## 2026-09-19 予定APIの可視I/O境界修正

- PR #238はc661336のCI 35423450105が成功。実CodeRabbitレビューで重複JSONキーの問題が見つかり、再現回帰を追加してac4603eで修正。現在は新headのCI 35423759752を確認する。旧CI成功だけでmergeしない。
- agenda_importのadd結果を内部recordとして予測へ見せていた点を修正。実返答のagenda-ackだけを使い、保護値はqueryで実際に返った後にだけ入力へ反映。内部snapshotは監査のみ。source_version closed-agenda-io-v2。
- 旧collection-v1は予測有効性の根拠に使わない。同じ実captureから別のcollection-v2へ再封印、1関連群/2roots/6prefix/12枝。照会前0/1は実測で環境ID以外が同じ、同一環境の回帰ではprefix全体同一を確認。compare trainはinconclusive_do_not_adopt、新trial/model/独立追加0。
- agenda-visible-prefix.md/results/agenda-visible-prefix-20260919.json参照。真値selection/unknownは維持。次は閉じたAPI/入力domainの依存関係契約。#204 Goal継続。

## 2026-09-19 閉じた予定APIのJSON値依存契約

- PR #238は修正head ac4603eのCI 35423759752成功後、main c565190へ統合。重複JSONのレビュー指摘は回帰で再現・修正し解決済み。修正後CodeRabbitはrate limit。
- PR #239をmainへ変更し、head de01cd2。新CIは35424098907/35424098235（同head、push/base変更で起動）。旧455c3b1向けCIだけでmergeしない。
- checked_agenda/read_checked_captureとtask_catalog --checked-agenda-capturesを追加。固定API/transportコード形、plain JSON domain、実state/返答の各field、有限介入とのbinding、辺の過不足を検証。限定したjson_projectionだけ既知にし、通常importのunknownは維持。
- source f3b1a0fで既存資料を再検証しchecked collection封印。1関連群/2roots/6prefix/12枝、全train。h4公有no、保護observe yes、保護enforce unknown。compare trainはinconclusive_do_not_adopt、新trial/model/独立追加0。
- checked-agenda.md/results/checked-agenda-20260919.json。一般の意味的依存・タイミング/例外・controller侵害は対象外。image同一性や独立holdoutの証明にしない。#204 Goal継続。

## 2026-09-19 生成モデルと予定API試行の紐付け

- PR #239はhead de01cd2のCI成功後main a0ae81cへ統合。PR #240はmainへ変更しhead7692937、現CI35424555217確認中。旧91db123向けCIの成功だけでmergeしない。
- agenda_plan_provider/generated_agendaを追加。1call/60秒/16KiBの事前予約→費用/CLI receipt保存→封印/再読込→trial前intent→API実行→import/通常・checked collection→generator strataを接続。生成は閉じた操作順/exportだけで、自由tool探索/再計画ではない。
- source1370a78、CLI0.153.4/requested gpt-5.5で1call成功、add/query/send+public。input3669/cached0/output63、receipt6778ms/controller6803ms。固定モデル版/価格は未確認。
- 生成計画から1バッチ12試行/15.9031秒/107379bytes、各最大20/180秒/1GiB。observe/enforce公有到着・保護到着なし。既存介入再利用で追加介入trial0。
- 1関連群/1root/3prefix/6枝、全train、receipt1。compare trainはinconclusive_do_not_adopt、独立追加0。generated-agenda.md/results/generated-agenda-20260919.json参照。#204 Goal継続。

## 2026-09-19 BFCLの公開ソース監査

- PR #240はhead 7692937のCI 35424555217全成功後、main 5044dbdへ統合。CodeRabbitはrate limitで実レビューなし。
- PR #241はhead cda8fa4のCI 35425083862全成功後、main 833543dへ統合。CodeRabbitはrate limit。
- BFCLをcommit 6ea57973へ固定し、LICENSE/データREADME/評価器/Message/Ticket/FileSystem実装など9ファイルをblob/SHA-256照合して静的監査。対話取得・上流コード実行・model/trialは0。
- Message/Ticketの19操作にresponse定義があり、コードの公開操作名と一致。一方、sendは内部inbox更新、ticket認証は非空欄確認で、送達・所有者境界の証明ではない。上流runnerの動的import/evalはホストへ接続しない。
- READMEのbase/augmented件数には共通操作列の派生がある。独立群数に読み替えず、直接F02へ採用しない。bfcl-source-audit.md/results/bfcl-source-audit-20260919.json参照。独立追加0、#204継続。

## 2026-09-19 MBPP開発候補の抽出

- 2026-09-19 追記: PR #241はhead cda8fa4 / CI 35425083862全成功後main 833543dへ統合。CodeRabbitはrate limit、実レビューなし。
- MBPPの固定commit 4700efb9/データSHA ccf64ceaを検証するmbpp_candidatesを追加。上流training374件だけを静的解析し、単一関数+JSON literal等値テストの199候補/597例を取得。元コードは実行せず、候補を安全性許可・独立群・真値として扱わない。
- 実結果はmbpp-candidates.md/results/mbpp-candidates-20260919.json。source c4eefb5、モデル/試行/採用独立群0。次は少数候補の全3例を隔離Dockerの有限バッチで参照結果と照合する。元データ/既存実測を変更せず、#204継続。
- 2026-09-19 MBPP有限実行: mbpp_batchは固定原版SHA+選定レコードhashで602/603/604だけを許可し、各3例・計9試行を別々のoffline/非特権/read-only containerで実行。予約・実出力を保存し、不一致/失敗も保持。任意候補の実行許可ではない。
- source56277da / mbpp-reference-v1で9試行5.6461秒/40519bytes、全期待結果一致。全保存出力・script/予約/hashを再照合。`research/flow_forecast/mbpp-reference.md` と `research/flow_forecast/results/mbpp-reference-20260919.json`。model0/採用独立群0、guard/receiver/F01未接続。次は元処理と由来を維持してtool I/Oと受信観測へ接続する。#204 Goal継続。
- 2026-09-19 MBPP tool I/O: PR #243はhead9262ce3のCI35425862275全成功後main d5fa73cへ統合。CodeRabbitはrate limit。PR #244はmainへ変更しheadd1ce46a、新CIを確認する。
- mbpp_transport/collectionを追加。固定602/603/604の第1例を実compute、結果と人工privateメタデータを保存、public/get→実返答sendを実MCP形式guardとreceiverへ接続。compute ackに内部privateを混ぜない。元MBPP自体の秘密/脆弱性を主張しない。
- source1858dd4で各課題public/private計6バッチ72試行、76.4875秒/630673bytes。public両mode到着、private observe到着/enforce送信前deny・未dispatch。保守的payload拒否でありexact flow予測の改善証明ではない。
- 36論理呼出予約・33dispatch/observer・9receiver送信bodyを再照合。mbpp-collection.md/results/mbpp-collection-20260919.json。モデル0/採用独立群0、F01未接続。次はstrict readerとcollectionへの接続。#204 Goal継続。
- 2026-09-19 MBPP評価取込: mbpp_import/read_capture、task_catalog --mbpp-captures/--mbpp-source追加。固定source/record/ケース、実script/guard/state/receiver・予約/resourceを検証。内部stateをprefixへ入れず、compute ackと実照会返答だけ反映する。
- source2d83025で既存6バッチをcollection-v1へ封印。3design/1関連群/6roots/18prefix/36枝、全train・h4全unknown・生成証跡0。plan-v1はcollection未指定のためevaluateで拒否、report未作成。旧planは保存し、collectionを結び付けた別plan-v2でtrain比較しinconclusive_do_not_adopt。
- mbpp-import.md/results/mbpp-import-20260919.json。新model/trial/採用独立群0。905関連テスト成功。次は計算と公有/private投影の限定した依存契約を接続。#204 Goal継続。
- 2026-09-19 MBPP入力能力契約: mbpp_contractで単一関数/局所名/固定primitiveのみをAST検査し、ambient参照・反射・alias・組み込み再定義等を拒否。正常なplain JSON経路限定で、実行許可/停止性/field因果/一般意味の証明ではない。
- source910eabeで実3参照と既存6captureをstrict reader経由で結合。root/report/dataset hashを保持。mbpp-contract.md/results/mbpp-contract-20260919.json。全関係unknown維持、旧seal/plan不変、新model/trial/採用独立群0。
- 927関連テスト成功後、capture binding回帰を追加して対象23件成功。次は入力/privateメタデータ変更の有限観測と固定wrapper投影契約を結び付ける。PR #244のCIは全成功、CodeRabbit run e0e36ee6-fc17-4427-be53-8438e69a8b65がreview in progressのため完了を確認する。#204 Goal継続。

- 2026-09-19 MBPP有限介入: PR #244は修正head5eb6fe4のCI35427001453全成功後main e5d82d8へ統合。実CodeRabbitレビューの文書パス指摘を修正・解決済み、修正後はrate limit。PR #245はmainへ変更しhead4ae7f52、新CIを確認する。
- mbpp_projection/batch/evidenceを追加。固定3課題にbaseline/private変更/input変更を各3呼出で実行。source9a8de05で3明示バッチ27試行/7.094秒/128844bytes、全一致。reader db7624dで全予約/実出力/実装を検証し既存6captureへbinding成功。
- mbpp-projection.md/results/mbpp-projection-20260919.json。全6bindingでimage IDは異なりidentical_image_verified=false。有限観測のみで普遍的非干渉や真値へ昇格しない。新model/採用独立群0、#204 Goal継続。次は固定wrapperと入力domainの投影契約。

- 2026-09-19 MBPP限定値依存: checked_mbpp/task_catalog --checked-mbpp-captures追加。固定参照の入力能力・wrapper・実state・有限介入を結び付け、公開引数→結果、privateの別field保存/public/full投影を検証。通常importはunknown維持、可視prefixと検閲を保持し辺の過不足を拒否する。
- source3bd1e69で既存6captureからchecked collection-v1を封印。1関連群/6roots/18prefix/36枝全train。h4公有18 no/private observe9 yes/enforce9 unknown。compare trainはinconclusive_do_not_adopt。946関連テスト成功、新model/trial/採用独立群0。
- checked-mbpp.md/results/checked-mbpp-20260919.json。#204継続、次は生成計画の実receiptをMBPP試行へ結び付ける。PR #245 head4ae7f52のCI35427867766は確認中、CodeRabbit rate limitで実レビューなし。

- 2026-09-19 MBPP生成receipt: PR #245はhead4ae7f52/CI35427867766全成功後main eb4a0aaへ統合。CodeRabbit rate limit。PR #246をmainへ変更、headad1516a/CI35428204677確認中。
- generated_mbpp/mbpp_plan_providerとcollector/import/generator_strata接続。sourcee3260c8で1call、gpt-5.5指定、input3728/output73/controller5831ms。compute/query/send+publicから1明示バッチ12試行15.676秒105419bytes、両mode公開到着・保護なし。旧介入再利用、新介入0。
- 1関連群/1root/3prefix/6枝、receipt1を検証。今回splitはcalibrationのみでtrainがなく、prepare/evaluateはno_fixed_distribution_training_rootsで拒否、出力なし。root/割当てを作り直さず既存記録保持。開発済み課題なので未使用調整群の証明ではない。generated-mbpp.md/results/generated-mbpp-20260919.json。
- 958関連テスト/Ruff成功、固定モデル版・価格は未確認、採用独立群0。#204継続。次は関連群/収集前割当てをcollection横断で固定し、既知課題の別splitを未使用扱いにしない経路を確認する。
