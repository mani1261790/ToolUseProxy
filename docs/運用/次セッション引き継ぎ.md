# 次セッション引き継ぎ

更新日: 2026-09-18

## 最新の実装状況

- #133はPR #201をmain d8358d4へ統合・Issue/Project完了。最終head 6a507a9の
  CI 35314274355はPython 3.11/3.12・macOS・配布再現性すべて成功。
  CodeRabbitは統合時点でレビュー中。後着コメントは次の継続で確認する。
- 現在は#134、PR #202。専用worktree `/private/tmp/tooluseproxy-issue-134`、
  branch `codex/issue-134-forecast-baselines`。4基準方式、確率・未来経路・早期警報の
  採点、固定評価計画、最終試料数・費用・採用条件を実装。関連84試験とRuff成功。
  全体試験・CIは進行中。Issueはまだcloseしない。
  [基準方式と評価条件](未来予測の基準方式と評価条件.md)にコマンドと凍結規約を記載。
  人工実行の証拠は `/private/tmp/tooluseproxy-134-evaluation-plan-v1.json`、
  `tooluseproxy-134-heldout-v1.json`、`tooluseproxy-134-training-smoke-v1.json`（同ディレクトリ）。
  F01の独立2群ではtestは空。学習データの採点を精度や採用の証明にしない。

- #140はmain 48d1da9へ統合。CI 35311887335の4ジョブ成功、ローカル全体1795件成功、
  2 skip、1609 subtests成功。48形式のDocker実通信は公開literal4形式の到達・完了、
  他44形式の実行前拒否、保護到達0、全5ケースの独立対照成立。


- #140の限定Python HTTP形式を保護情報照合へ接続。人工Docker旧新版比較で
  公開送信の誤停止が改善し、保護停止対照も成立。PR #200で統合・Issue完了。
  [検証範囲と証拠](Python固定HTTP送信の検証.md)に対応形式と制約を記載。

- #188はPR #192をmainへ統合済み（3c899c0）。人間の実機受入は最後に残し、Issueはopen。
- #189のPR #193は人工再生画面を作っており、利用者の目的と不一致だったため再open。
  実際のevents.dbへ接続し、ToolCall・I/O・ブロック判定をリアルタイム表示する
  読み取り専用ビューアーへ修正。起動は[展示画面](../exhibition/README.md)。
- #144はPR #194で統合・Issue完了。
- #130はPR #195で統合・Issue完了。実Codex + Dockerで拒否後の別案と上限終了を確認。
  [Codex人工探索](Codex人工探索.md)に、版・受入条件・実証と模擬試験の区別を記載。
- #189のライブDBビューアー修正はPR #196で統合済み。
- #131は新しいDocker環境での再現・短縮・旧新版比較を接続し、
  2操作から1操作への短縮と、旧新版で同じ誤停止が残ることを実通信で確認。
  [再現と修正前後比較](再現と修正前後比較.md)に実行・保存・受入範囲を記載。
  #131はPR #197で統合済み。 #132のIssue連携はPR #198で統合済み。
  続いて#140の誤停止修正、#133〜#138を進める。
  探索だけで「自動探索と改善一式」が完成したとは扱わない。
- #132の専用outbox・共有内容検査・既存Issue対応を実装。
  #140への実投稿は旧新版各1件、再実行は追加0件。
  [人工試験のIssue連携](人工試験のIssue連携.md)に状態と検証範囲を記載。
- PR #197の後着レビューも修正。policy版不一致の比較拒否、根拠なし指紋の拒否、
  短縮完了時の予算表示、CLI既定600秒、古い引き継ぎ指示を回帰試験付きで整理。


## 最新の利用者指示：人間の確認は全機能実装の後へ

- #188の機能実装・CIはPR #192に揃っている。管理者環境での実機承認は未検証として
  [人間による最終確認](人間による最終確認.md)へ残し、後続実装の開始条件にしない。
- #189、#144、#130、#131は統合済み。現在は#134の基準方式と採点器。次は#135〜#138。
- 未検証の項目を合格としてcloseしないが、そのIssueを理由に他の機能実装を止めない。
- #189はPR #196で実DBのライブ表示へ修正済み。#144はPR #194、#130はPR #195、
  #131はPR #197で統合・Issue完了。
- 以下の「#188完了を待つ」記述は旧方針。この最新指示を優先する。

## 以下は#188実装中の経過記録（最新の進め方は上記を優先）

## IssueごとのGoalで順次実装（利用者の追加指示）

- Codex Goalは#188をactiveとして作成済み。各Issueについて実装・検証・CI・
  PR統合を完了し、IssueとProjectを完了にしてから、そのGoalをcompleteにする。
  次のIssueで新しいGoalを作る。未完了のIssueを先送りしてcloseしない。
- 順序: #188 → #189 → #144 → #130 → #131 → #132 → #133〜#138。
  関連#140は自動探索と改善一式の受入条件に照らして扱う。
- #188の続きで、OS管理者所有の独立した状態管理とHookのleaseを実装中。
  `authority_state.py` は対象・世代・操作を照合し、停止中の新規Hook受付を止め、
  既存Hookが残る間はdeactivating、終了後にinactiveへ遷移する。
  元ファイル・検出DB・保護リストは変更しない。旧形式からの暗黙の登録は禁止。
- これは承認機構の完成ではない。管理者用の承認入口、改変不能な導入物、
  ACLを含む実機の権限検証、非同期workerの扱い、再初期化導線がまだ必要。
  通常CLIのapply拒否は維持。実機の管理者領域への導入はしていない。
- PR #192の初回CIは配布再現性・macOS smoke成功、Python 3.11/3.12で失敗。
  3.11は既存externality dogfoodの `hook_p95_budget_exceeded`、3.12は既存redactionの
  32件境界試験でeligible期待に対しrejected。統合前に診断し、合格を確認する。
- 状態管理・Hook連携の対象試験97件成功。保護リストを含まない独立checkoutで
  全体1454件＋補助1606件成功、2件skip。Ruff・compileallも成功。
  redactionの件数境界試験は時計を固定し、時間制限の専用試験を維持した。
  変更後のredaction試験34件＋補助17件も成功。遅延上限は変更していない。
- PR #191の2件の文書指摘を修正し、CI全件成功後に統合済み
  （main: `2ca2a8c`）。PR #192はmain向けDraftへ変更。
  次は管理者用承認入口・導入物・worker連携・再初期化と実機検証を進める。
- 次のGoal継続で、管理者用入口 `authority_admin.py` と単一ファイル生成
  `scripts/build_authority_admin.py` を追加。固定のroot-owned導入先とisolated/no-site
  Pythonを要求し、通常ユーザーの呼出しは拒否する。確認期限120秒、対象のowner・
  canonical path・inode・世代を検査し、取消・期限切れ・差替えを拒否する。
  入力による確認はOS認証とは区別する。中断後はstatus、既存停止処理のdrainはfinish。
- [管理者導入手順](Unsetup管理者導入.md)を追加。管理者の認証情報・root shell・UI・
  sudo委任へエージェントが到達できる構成は不可。Macへの導入は未実施。
  生成物はMac同梱Python 3.9.6のisolated/no-siteでもhelpと未承認拒否を確認。
- `1f6a6ff` 時点のPR #192 CIは全件成功。新しい変更は別commitで検証する。
  次の実装はworker連携とCLIの停止状態表示・再初期化導線。その後、人工対象で
  管理者権限・ACL・実際の人間の承認を確認する。#188は未完了のまま。
- `c2ba1b6` のCIも全件成功。CLIは管理者停止中にDB・保護リストへ触れず状態を返し、
  init/setup/protect等の実行を拒否する。設定は未調査と明示し、未設定とは扱わない。
  activeなCLIの処理中はleaseを保持し、停止との競合時は終了までdrainする。
- 外部分類workerも対象projectのleaseをprovider呼出しと結果保存まで保持する。
  inactive/deactivatingのジョブは変更せず飛ばし、他projectの処理を続ける。
  管理者状態が導入済みなのにworkspace identityを解決できない場合は送信しない。
  対象回帰148件＋補助4件成功後、identity不一致・未登録の拒否試験も追加。
  次はpilot同期と自動整理のworker連携。ここまでは人工fixtureの検証であり、
  実機導入・人間の承認・全worker停止の受入確認ではない。
- pilot比較の準備・同期にも参加project全員のleaseを接続。
  停止projectを含む比較はprepare/outboxを変更せず保留し、他の比較は継続する。
  participant aliasとworkspaceの対応が解決できない場合は送信を拒否する。
  同期中は認証・GitHub照会・送信・ローカル結果保存までleaseを保持する。
  人工fixtureと偽transportによる混在project・同期中停止・無通信・不正identityの
  対象試験60件成功。実GitHubへの試験送信はしていない。
  次は自動整理と複数projectを参照する比較生成の停止境界を確認・接続する。
- 比較生成にも停止境界を追加。停止済みprojectのStopはcoverage取得前に終了する。
  他projectのStopから比較を作る際も停止projectを入力から除外し、参加projectの
  leaseを比較結果のcommitまで保持する。元の観測記録は削除しない。
  3 project中1件停止・残り2件で比較・比較中停止のdrain・観測60件保持を人工試験。
  pilot生成/同期の対象25件とRuff成功。次は自動整理の候補選定と削除の両方へ接続。
  `storage_cleanup.py` の `_retention_candidates` はplan/apply双方で使われ、
  scoped sessionだけでなくsessionなしevent・共有migration backupも扱うため、
  停止projectの履歴をそれらの経路から削除しないことも確認する。
- 自動/手動整理共通のplan/applyへ管理者状態を接続。停止projectのsessionと
  sessionなしeventを候補から除外し、削除transactionのcommitまでactive projectの
  leaseを保持する。所属不明の記録も管理者状態導入時は保持する。
  共有migration backupは過去の参加projectを証明できないため、管理者状態導入時は
  `cleanup_blocked` とし削除保留。現役DBの他projectの期限切れ記録は整理できる。
  停止前の整理計画は停止後にrevision不一致で拒否される。
  人工fixtureの対象78件＋補助3件、Ruff成功。実データの整理・削除はしていない。

## #188 承認設計の着手結果（2026-09-18）

- branch: `codex/issue-188-unsetup-authorization`。PR #191 の branch から分岐し、
  Draft PR #192を作成。承認経路未実装のため#188をcloseするPRではない。
- 開始時のGoalは未設定。新しいGoalは作成していない。
- [承認設計ADR](../adr/0005-Unsetupの承認をエージェントの実行権限から分離する.md)を追加。
  CLI自身の確認番号・TTY・環境変数では承認できない。管理側のコード・状態・
  Hook設定までエージェントの書込権限から分離することを適用の条件とした。
- `unsetup plan` はDB・保護リストを開かない日本語プレビュー。
  現在の有効状態は未調査と明示する。`unsetup apply` は常に拒否し、変更しない。
- #188 は未完了。独立した管理側の承認・適用、同時Hook・再初期化・実機確認が残る。
  故障時の暫定復旧は利用者によるPlugin管理画面の手操作。エージェントは代行しない。
- 検証: 保護リストを含まない独立checkoutで全体1423件＋補助1606件成功、2件skip。
  対象回帰84件＋補助4件、Ruff、compileall、diff checkも成功。
  CI・実利用への導入・人間による解除の実機証拠とは分ける。
- PR #191 はCI全件成功、未統合。レビューに退避削除の証拠の記述と、下記の旧手順
  5〜6の順序矛盾が指摘された。旧手順はこのbranchで修正。退避の実データは調査していない。
- Project #1 の実装順は #188 → #189 → 自動探索と改善一式 → 将来予測 → #190。
  #188をIn Progressへ更新・読み戻し済み。#189はTodo。
  概要readmeには古い順序・alpha.18の状態が残る。
- Pluginは利用者申告どおり無効のまま扱い、設定操作はしていない。
  rootの保護リストは閲覧・編集・削除・stage・commitしていない。

## 最優先の引き継ぎ（2026-09-18）

### 次のセッションで再開する場所

- 作業branch: `codex/exhibition-roadmap-20260918`。文書整理のPR #191は未統合。最初にレビュー・CIと差分を確認する。
- Project #1の「実装順」は設定・読み戻し済み。#188 → #189 → 自動探索と改善一式 → 将来予測 → #190の順を維持する。
- 次の実装対象は#188。ただし利用者から「エージェントにUnsetupを悪用されたら保護が終わる」と指摘があった。正規CLIの構文・対象確認だけでは利用者承認を証明できない。
- Unsetupの変更前計画は読み取り専用にできるが、適用にはエージェントが生成・偽造・再利用できない利用者承認の経路が必要。`--yes`、会話中の自己申告、エージェントが読める確認番号だけで解除可能にしない。実際に利用可能なCodex/OSの承認機構を調べ、未承認・偽装・再送・別projectの拒否を試験する。
- 故障時の人間による解除は必要だが、無条件の管理命令許可とは別。信頼できる承認経路が提供できなければ、適用だけ利用者の手操作にする。同一ユーザー権限の任意shellでローカルファイルを書き換えられる環境では、CLIだけで悪意あるエージェントへの完全な耐性を保証できない。保護境界を明示する。
- Plugin alpha.24は利用者が無効化済み。自動で再有効化しない。今回の依頼は引き継ぎまでで、Unsetup実装はまだ開始していない。
- 新規Goalを作る前に現在のGoalを確認する。旧#176 Goalの残存を、新しい優先順位の全機能完了と取り違えない。

次は **#188 Unsetup → #189 展示画面 → #144/#130/#131/#132 自動探索と改善一式 → #133〜#138 将来予測 → #190 Jev比較**。展示が近いため、固定人工試験の再生で先に展示を成立させ、自動探索の完成を待たない。詳細とデータ整理結果は[最新の実装順](2026-09-18-実装順とデータ整理.md)を参照。

alpha.24の導入・有効状態は9月18日に確認済み。下記のalpha.22無効化・再起動待ちは過去の経緯であり、現在の操作指示に使わない。容量対策の主要実装と初回整理は実施済み。旧v7退避は手動削除、v11退避は専用フォルダーへ移動済み（利用者によるその後の削除は未確認）。現役DBはquick_check合格。

#153/#159の最終実動作確認と自動整理の延期継続、#167/#169は未解決の残件。未確認の保護成功を主張せず、展示前の全件解決を前提にしない。rootのprotected_sources.jsonを閲覧・変更・削除・commitしない。

以下は9月9日までの履歴。作業順は上の最新順を優先する。

9月18日の追記: alpha.24でもProject更新とgit diffの確認がhook_deadline_exceededで実行前に停止した。利用者がPluginを無効化し、enabled=falseと通常操作の復帰を確認した。現在は無効のまま。#188では、判定時間切れ・DB使用中・破損などでも利用者による正規のUnsetupが停止されないことを必須とする。Unsetup追加だけで判定の遅延原因が直るとは扱わない。

## 最新の依頼と計画の状態

容量増加への対策を、自動攻防試験より先に進めています。親Issue #153の下で、#154「変更前計画」、#155「保存重複削減」、#156「30日を過ぎた操作記録の安全な整理」、#157「更新前DBの退避整理」、#158「タスク終了後の自動整理」は完了しました。#158では、初回確認前は無効、タスク終了時のHookは予約だけ、別処理は前回開始から24時間に1回・20作業単位までとしました。DB使用中、別整理中、監視処理が始まった場合は延期または未確定処理を中断します。利用者の実DBでは初回整理まで完了し、操作記録と退避を削除せずDB本体の未使用領域だけを縮小しました。

容量対策は#154 → #155 → #156 → #157 → #158 → #159の順です。現在は#159の最終確認中です。`0.1.0-alpha.22`を公開し、`public-alpha`と現在のCodexへ導入しました。現在のPluginは利用者が無効化した状態を保っており、有効化とCodexの完全再起動後の実経路確認が残っています。設計ではschema v12移行前にも計画と利用者確認を求めていましたが、計画命令はv12だけを受け付け、v11では安全停止しました。実作業では更新前DB全体を残してv12へ先に更新し、その後、削除を伴わない最初の変更前計画を表示しました。設計どおりの順序ではなかったことを隠さず、旧形式のまま移行前計画を出す修正をIssue #169へ分離しています。30日以内の操作記録、改善用フィードバック、利用者評価、設定、保護対象登録は容量だけを理由に削除しません。

### alpha.19の公開・導入と初回計画

- PR #165を統合し、`v0.1.0-alpha.19` prereleaseと7ファイルを公開。公開物のoffline検証とSHA-256照合に合格
- PR #166で`public-alpha`を公開タグと同じ内容へ更新。現在のCodexへalpha.19を導入し、有効状態を確認
- 実利用DBをschema v11からv12へ更新。更新前DB退避を作成し、既存の保護リストは作成・変更していない。ただし移行前確認を実装できず設計順序から外れたため、Issue #169で修正する
- 初回の`storage cleanup plan`は保護リスト読取なし、DB変更0、source変更0、network使用なしで完了
- 30日超で現在削除できる操作記録は0件。詳しい操作記録約1.43 GB、再作成可能な検出用data約1.72 GB、改善用feedback約0.89 MB、長期設定約0.18 MB
- DB本体約4.81 GB、そのうち未使用page約1.66 GB。更新前DB退避は2件・約7.50 GB、管理対象合計は約12.31 GB
- 更新前DB退避2件は、現在版の監視処理確認前なので削除不可。DB縮小には約4.83 GB必要で、計画時の利用可能量約20.65 GBは条件を満たす
- 1回目の計画作成に約11分かかったため、性能改善をIssue #167へ分離しProject #1へ登録
- 利用者は計画を明示確認したが、取り直した2回目の計画作成には約14分かかり、表示直後の自動整理enableが`automatic_cleanup_plan_expired`で安全に拒否された。期限を計画開始時刻から数えていた不具合をIssue #170へ分離し、整理、backup削除、DB縮小は未実施

### alpha.20の修正・公開・初回整理

- Issue #170 / PR #171で、計画の表示完了時刻`reviewed_at`から5分を数え、境界時刻・表示完了時刻・確認番号を一組として照合するよう修正
- `v0.1.0-alpha.20` prereleaseを7ファイルで公開し、PR #172で`public-alpha`を同じtreeへ更新。現在のCodexへ導入し、Plugin版`0.1.0-alpha.20`、CLI版`0.1.0a20`、有効状態を確認
- 全試験1,381件、補助1,606件合格、2件skip。Python 3.11 / 3.12、macOS package、再現可能な配布候補、公開元検査finding 0に合格
- 実DBの変更前計画は、30日超の操作記録0件、削除可能な更新前退避0件、DB整合性正常、縮小用空き容量十分。保護リスト未参照、DB変更0、source変更0、外部通信なし
- 自動整理を有効化し、初回整理を1回実行。操作記録0件、退避0件を削除し、DB本体だけを4,811,567,104 bytesから3,049,226,240 bytesへ縮小。1,762,340,864 bytesを回収
- 縮小後の未使用pageは0 bytes、DB整合性正常。更新前DB退避2件・7,500,648,448 bytesは現在版のHook確認前なので維持
- 自動整理は有効、次回実行可能時刻は2026-09-09 03:23:33 JST。管理対象全体は約10.55 GBで4 GiB基準を超えるため要対応通知が残るが、主因は維持中の更新前DB退避
- alpha.20時点では、#159の残件はCodexを完全に終了・再起動した新しいタスクで、Hook到達、保護対象の実行前停止、通常操作の復帰を確認することだった。現在の確認対象はalpha.22

### alpha.22の時間切れ復旧と導入

- Issue #176 / PR #177で、自動整理の全体調査中に新しいHook操作が始まったら調査を中断し、通常利用を優先するように修正
- DBが未作成、破損、旧形式、または書き込み中でも、正規launcher・現在project・専用領域と厳密一致する管理操作だけを復旧経路として許可。追加shell命令や通信を付けた類似命令は安全停止を維持
- 外部送信先でないと確定したローカル操作は記録を残しつつ重い全体判定を省略。後の外部操作で必要なつながりを失わないよう読取位置は進めない
- 確認用番号の引数順を変えても、正規CLIが受理する並びなら「今回の操作」のHook到達確認を省略しない回帰試験を追加
- Issue #178で、1万候補のオフライン性能試験が共有CI負荷で500〜600 msを揺れる問題を修正。警告上限を1,000 msへ調整しただけで、精度・安全性・再現性の基準は変更していない
- 全体試験1,390件・補助1,606件、追加の類似度試験8件・補助64件、Python 3.11 / 3.12、macOS導入、配布物再現検査に合格
- `v0.1.0-alpha.22`をGitHub事前公開版として7ファイルで公開し、再ダウンロード後の検査に合格。PR #179で`public-alpha`を公開タグと同じ内容へ更新
- 空の分離Codex設定でGitHubからalpha.22を導入できることを確認。現在のCodexもalpha.20からalpha.22へ更新し、旧版cacheを除去。利用者が無効化した状態は維持
- #159の残件は、利用者がPluginを有効化し、Codexを完全に再起動した新しいタスクで、Hook到達、保護対象の実行前停止、通常操作とToolUseProxy管理操作の復帰を確認すること

### #158の完了証拠

- 自動整理は初期状態で無効。変更前計画の表示完了時刻から5分以内で、境界時刻・表示完了時刻・確認番号が一致した場合だけ有効化
- タスク終了時は小さな予約ファイルを作り、別処理を起動してHookを待たせない
- 前回開始から24時間に1回、20作業単位まで。日付変更だけでは再実行しない
- DB使用中、別整理中、監視処理の直後は延期。削除中の監視開始はSQLite処理を中断して未確定変更を元に戻す
- 強制終了時の実行中状態を保持し、次回に失敗通知を残して24時間後に安全に再開
- DB縮小は空き領域が十分な場合だけ行い、監視開始時は中断。終了後に追記領域整理とDB簡易検査を実行
- 2 GiB以上を注意、4 GiB以上を要対応として本文非表示で記録。正常時は通知せず、失敗、4 GiB以上の継続、空き容量不足だけを次回開始時に1回通知
- `status`で前回日時、削除・残件数、回収容量、延期理由、次回実行可能時刻を確認可能
- 全体1380件合格、2件スキップ、補助1603件合格。配布物・アンインストール関連15件合格、1件スキップ
- #158完了時点では、利用者の実DBでの自動整理有効化、削除、DB縮小は未実施。最初の計画確認と適用は#159へ引き継いだ

## 自動攻防試験の完了済み履歴

容量対策を始める前に、[実装計画](自動攻防試験と将来経路予測の実装計画.md)を親Issue #126、子Issue #127〜#138、通常利用開始の確認 #139へ分割しました。通常利用開始の報告地点は完了しています。当時の公開・導入済み版は`0.1.0-alpha.18`でした。#127 → #128 → #129 → #112 → #113に加え、追加修正#143、通常利用開始の確認#139、個別解除と自己ブロック修正#149を完了しました。容量対策の開始時に公開・導入済みだった版は`0.1.0-alpha.19`で、実利用DBはschema v12へ更新済みです。Dockerでの固定実通信11試行と7つの対照確認は[人工試験環境](人工試験環境.md)を参照してください。残る#144と#130以降は、容量対策#159が終わった後に再開します。

PR #141・#142のalpha.16に対し、統合後に届いたレビューで質問の複数項目へ分割した保護文字列の見落としを再現しました。#143 / PR #145で表示全体の照合を加え、PR #146で配布経路へ反映し、alpha.17を公開・導入しました。未修正のalpha.16を通常利用開始の最終版にはしていません。

### alpha.18の完了証拠

- #149 / PR #150で、登録済みの保護対象を1件だけ外す`protect remove plan / apply`を追加
- 元ファイル、ほかの登録、利用者が追加した未知の項目を残し、変更前の保護リストを専用領域へ権限`0600`で保存
- 確認後に保護リストが変わった場合、対象pathや確認番号が異なる場合は変更しない。再送は`already_removed`として安全に終了
- 正規launcher、現在のCLI構文、同じproject、同じ専用領域を満たすローカル管理操作は、ToolUseProxy自身が停止しないよう統一
- 外部通信を行い得る`externality process`と`pilot sync`、追加shell命令、別project、不正な引数は自己操作として許可しない
- 全体1323件合格、2件スキップ、補助1601件合格。Python 3.11 / 3.12、macOS導入確認、再現可能な配布物確認が合格
- `v0.1.0-alpha.18`を公開し、`public-alpha`と公開タグの内容一致を確認。現在のCodexへalpha.18を導入して有効化済み
- 導入済みの実物と人工workspaceで、個別解除、再送、元ファイル保持、他登録保持、非公開backupを確認
- 導入済みHookへ状態確認、設定表示、個別解除確認を入力し、3件とも停止されないことを確認
- repository rootの利用者管理`protected_sources.json`は内容取得、変更、削除、stage、commitをしていない。実際の`README.md`登録も変更していない

現在開いているタスクはPlugin更新前に始まっているため、新しいHookがこのタスクへ読み込まれた証拠にはしません。実利用ではCodexを一度再起動し、新しいタスクから解除対象を指定します。

### alpha.17の完了証拠

- 全体1314件合格、2件スキップ、補助1601件合格。Ruffと必須CIが合格
- 公開元は`6570ea259f1821fbb1be8bcbebc7e7d6398f2003`。公開タグと`public-alpha`は同じ内容
- cleanな専用checkoutでソース監査の指摘0、配布物検証、隔離Codexでの導入・更新・復帰試験が合格
- 公開7ファイルのハッシュがローカル検証済みファイルと一致。導入済み監視処理100ファイルも公開ZIPと一致
- 新しく起動したアプリ同梱CodexでGit一覧と質問表示を許可し、動的な外部操作は実行前に停止
- 同じ実行の確認コマンドが終了0となり、`current_invocation_protected_block_observed`、新版の照合、現在操作のHook到達を確認。開始1、操作前4、操作後3、終了1の記録
- 未設定の新規Git projectは5段階とも終了0・出力0、新しいCodex実行でも通知なし・記録0
- 5 Hookの確認済み状態を維持。信頼確認の回避や信頼設定ファイルの直接編集は行っていない
- root保護リストと、既存2件のworkspace設定revisionは作業前後で同一。実利用DBの削除・作り直しなし

利用者には、Codexを一度再起動し、新しいタスクで普段どおり使うよう報告しました。現在開いている古いタスクが新版へ切り替わったとまでは主張しません。alpha.16の初回確認コマンドで操作後記録を得なかった事例は#116へ記録し、終了時の定型文を成功証拠にしていません。

研究用処理の中断予約・再開・受信設定エラーの追加課題は#144で扱い、#130の自動探索より先に解消します。#130以降の継続運転はまだ起動していません。配布ZIP・wheel・sdistは研究処理を除外しますが、Git経由ではソース一式がコピーされます。通常の監視処理から研究処理を読み込まず、自動起動しないことと、ファイルの不在を区別してください。

### 今回の変更

- #127: ホストの実データ・認証を渡さない通信なしの実行領域を確認
- #128: 人工試験専用の記録、操作前予約、重複防止、途中終了後の未確定状態を実装
- #129: 受信側を独立させた固定HTTP試験を実行。停止判定、受信対照、故障注入を別集計
- #112: 質問文と選択肢だけの既知の関数操作を、全表示内容の照合後に許可。未知の形式や保護情報入りは停止を維持
- #113: 固定の`git remote`一覧表示をローカル操作として区別。送信・更新・動的命令へ許可を広げない
- #140: 人工試験で見つかったPython公開文字列送信の不要な停止を別Issue化。安全な解決方法が未実装なので保守的停止を維持

通常利用開始は研究全体の完成ではありません。固定の人工試験を、実モデルが試行錯誤する探索、任意命令の安全性、将来予測の有効性の証拠にはしません。

## 以前の完了地点: alpha.15

Codex対応、alpha.12の公開判定、実projectでの初回集計、最初の検出改善、実運用DBで見つかった時間切れ対策を`0.1.0-alpha.15`までに完了しました。以下はalpha.15時点の履歴であり、現在の公開・導入状態は冒頭を参照してください。

Issue #114では、番号を直接書いた単独の`gh issue view`を安全に読み取り操作と判断できるようにし、PR #118で取り込みました。Issue #119で`0.1.0-alpha.14`を公開し、現在のCodexへ導入しました。

その実確認中、3.4GBまで増えた保存DBが冷えた状態では操作前の判定に約12秒かかり、Codexの10秒制限で判定が打ち切られて操作が始まる問題を確認しました。Issue #122では次を実装しました。

- 現在の実行だけを読む索引を必ず使い、不要な全体走査を避ける
- 操作前の内部判定が7秒を超えた場合、途中結果を捨てて固定の停止結果を返す
- Codex側の操作前Hook制限を15秒にし、内部の停止結果を先に受け取れるようにする
- 必要な索引がないDBは、無理に検索せず安全側で停止する

PR #123を`main`へ取り込み、`v0.1.0-alpha.15`をGitHubの事前公開版として公開しました。PR #124で同じ内容を`public-alpha`へ反映しました。現在のCodexもalpha.15へ更新し、変更された操作前Hookを正規の確認画面から確認済みにしました。

新しいCodex実行で、設定済みのPromptPredictionでは次を確認しました。

- 番号を直接書いた`gh issue view 122 ...`は操作前判定を通過し、コマンドが開始した
- 変数を使った`n=122; gh issue view "$n" ...`は操作前に停止し、コマンドは始まらなかった
- 同じ実行番号で開始、操作前、操作後、終了の記録が保存された

未設定の新規Git projectでは、ToolUseProxyの案内を出さずにローカルコマンドが実行され、保存件数は0でした。rootの`protected_sources.json`は作業前後で同一です。

## 試験結果

- alpha.15実装時の全体試験: 1162件合格、2件スキップ、補助確認1596件合格
- 最終修正後の対象試験: 16件合格
- Python 3.11 / 3.12、macOS導入確認、再現可能な配布物確認: すべて合格
- 公開候補は`main`の取り込み結果と同一
- `public-alpha`は`v0.1.0-alpha.15`と同一内容
- 導入済みの監視処理とHook定義は公開版と同一

## 完了したIssue

- #99: 実project試行の設計と初回比較
- #101〜#106: 内容を保存しない評価、訂正、自動比較、改善提案Issueへの接続、実利用確認
- #107: 未設定projectでは通知・記録・停止を行わない
- #114: 固定番号の`gh issue view`で生じる不要な停止を減らす
- #119: alpha.14の公開・導入
- #122: 操作前判定の時間切れによる素通りを防ぐ

## 既存の未完了Issue

次は、初回比較で見つかった「外部への操作かどうかの判断」を、関数操作と端末操作に分けて改善します。

1. #144: 人工試験の中断予約・再開・受信設定エラーを厳密化する
2. #130〜#138 / #140: 自動探索、原因調査と比較、将来予測、人工試験で見つかった追加改善
3. #115: 観測できる範囲を確定できない問題を減らす
4. #116: 記録が不完全で原因を決められない問題を減らす
5. #36: 直接照合、送信内容の解決、情報のつながりを比較する
6. #54 / #24: 通信の観測と動的な端末命令の判断を改善する
7. #38: Git pushで実際に送られる内容を解決する
8. #46: ローカルの意味類似を観測専用で比較する
9. #37 / #39: タスク、別担当、Git境界での引継ぎを設計する
10. #55: 内容を保存しない監査と将来の学習評価を整備する

Issue #112と#113は別Goalで再現・実装・回帰試験を完了しました。公開・導入の状態は#139で別に管理します。

## 次のセッションで最初にすること

1. branch、`git status`、親Issue #153、Issue #159、Project #1、現在のGoalを確認する
2. [保存容量と自動整理](../設計/保存容量と自動整理.md)、[実装タスク](実装タスク.md)、[現行研究方針](../研究/現行研究方針.md)を読む
3. 完了済み実装を作り直さず、この文書冒頭の最新の実装状況から続ける。#188と#159の実機受入は最後に残す
4. 既存Issueを重複作成せず、各Issueを一つのGoalとして、実装、全体試験、PR統合、IssueとProjectの完了まで進める
5. #159は実動作確認の残件として保持する。初回整理を繰り返さず、Pluginを自動で有効化しない。再検証時はその時点の導入版でHook到達・保護対象の実行前停止・通常操作の復帰を確認する
6. 現在は#132。次に#140の修正と正常操作を含む比較を行い、自動探索・改善一式の確認後に#133〜#138へ進む

## 作業時の注意

- repository rootの`protected_sources.json`は利用者管理ファイルである。削除、編集、commitをしない
- 保護対象の内容、受け取った生データ、URL、接続先、絶対パスをIssueやCIの成果物へ出さない
- 意味類似だけの結果やAIの判断を自動許可へ使わない
- 判断不能な外部操作に保護対象由来の情報が届く場合は、安全側の停止を維持する
- Hookから見えない操作を保護済みと表現しない

## 新しいセッションへの開始文

> `/Users/mani/Developer/ToolUseProxy`の開発を引き継いでください。branch・git status・現在のGoal・Project #1と、この文書の最新の実装状況を確認してください。#132のIssue連携、#140の誤停止修正、#133〜#138を進め、人間による実機受入は最後に残してください。ToolUseProxyは無効のまま、root protected_sources.jsonは閲覧・編集・削除・stage・commitしないでください。

## #188 継続監査の追加（2026-09-18）

- `3f73fd2` の全体試験は独立checkoutで1499件＋補助1606件成功、2件skip。
  Ruff全体も成功。試験終了を確認して独立worktreeは削除済み。
- 管理者登録済みprojectのconfig set/unset、protect remove/reconcile/migrate applyを
  通常CLIで拒否し、管理者状態導入時のuninstall applyも拒否する。DB/保護リストの
  読取前に止める。通常CLIのrevision/tokenを人間の承認へ昇格させない。
- 管理者用入口は実装済みなので、Unsetupの日本語説明を「未実装」から
  「通常CLIに承認権限がなく、独立した導入済み管理者入口と人間操作が必要」へ修正。
  対象試験55件とRuff成功。直接ファイル編集やHook差替えは権限分離の未検証範囲。
- 管理者用単一ファイルをrelease candidateの4つ目のartifactへ追加。
  schema 2のmanifest/checksum/SBOMへ含め、wheel内の管理者ソースから再生成した
  byte列との一致をoffline検証する。旧schema 1候補の検証互換性も維持。
  独立checkoutで配布物/管理者入口21件＋補助5件、Ruff成功。
  候補生成・検証は権限昇格やMacへの導入を行わない。実機ゲートは未完了。
- `6fa1ff9` のclean独立checkoutから候補を実際に生成・検証済み。
  場所は `/private/tmp/tooluseproxy-188-rc-6fa1ff9`、artifact 4件、archive member 349件。
  管理者用ファイルSHA-256は
  `740d530e6a74519276b07b5d238dcec63cbef6e850907358ddb33179934343ec`。
  source用worktreeは試験終了後に削除済み。候補は管理者レビュー用に保持している。
- [実機受入手順](Unsetup実機受入.md)を追加。人間だけが操作できる管理者環境を
  用意できるか、利用者へ質問済み。回答待ち。現在のタスクのUIDは501で、
  管理者への昇格・導入・Plugin有効化はしていない。
- `6fa1ff9` CIのrelease candidate/macOS smokeは成功、Python 3.11/3.12全体は実行中。
  人間の管理者境界をエージェントが代行して「検証済み」にしない。
- 最新文書head `7e558ee` のPython 3.12 CIで120秒ちょうどの期限試験が失敗。
  実装head `6fa1ff9` のCIは全件成功していたが、浮動小数点の減算で経過120秒が
  119.99999999999999になる境界を再現できた。開始時刻8.003の人工時計で固定し、
  減算からdeadline自体との比較へ変更。期限120秒は維持。対象19件・Ruff成功。
  `/private/tmp/tooluseproxy-188-rc-6fa1ff9` は旧候補であり実機導入に使わない。
  修正版のcandidate生成・CI確認が次の作業。管理者環境の回答待ちは継続。
