# 次セッション引き継ぎ

更新日: 2026-09-06

## 結論

優先追加: [Issue #107](https://github.com/mani1261790/ToolUseProxy/issues/107)で、未設定projectへのPlugin初期化案内を修正しました。入口と起動scriptの両方で利用対象を確認し、対象外では5 Hookを無出力にします。明示設定時に専用保存領域の`events.db.workspaces/`へprojectごとの印を保存し、DBやPython起動が壊れても設定済みの保護を黙って解除しません。projectごとに分けることで、同時設定による上書きと、一つの印の破損が別projectへ波及することを防ぎます。入れ子になった別のGit repositoryも親の設定を引き継ぎません。全体試験1147件合格、2件スキップ、補助確認1590件合格です。

2026-09-06に作業中コードから配布物を作り、`tooluseproxy@tooluseproxy`として現在のCodexへ導入しました。5 Hookは通常の確認画面で1件ずつ出所と実行先を確認して信頼済みです。設定済みprojectの新規Codex実行では、公開文字列を表示する1コマンドに対して同じ実行番号の`session_start`、`pre_tool_use`、`post_tool_use`、`stop`をDBに記録しました。未設定projectの新規Codex実行ではToolUseProxyの案内を出さず、DBへの追加も0件でした。設定確認は合格し、既存の保存先を継続利用しています。rootの`protected_sources.json`は導入前後で同一です。配布元は作業中修正版を固定したlocal marketplaceへ一時的に差し替えています。コードのcommit・push・公開alphaへの反映は未実施です。

旧版は対象一覧を持たないため、DBの明示初期化・設定・保護リストの存在から対象を引き継ぎます。過去の自動観測行だけでは有効化と見なしません。旧DB自体が読めない、対象一覧が壊れた、Hookの作業場所が識別できない、Pythonを起動できない場合は、対象外だと断定できないため安全側の警告・停止を維持します。旧DBと対象一覧の両方を失った場合、以前の設定を復元判定することはできません。2026-09-06の導入時に明示setupを行い、既存の明示設定2件をprojectごとの印へ移行しました。

Codex対応は`0.1.0-alpha.12`で一区切りです。次のセッションでは、Plugin導入やDesktop dogfoodをやり直さず、ToolUseProxy本体の検出精度を改善します。

親Issueは[#99 実project pilotで検出精度とunknown分布を測る](https://github.com/mani1261790/ToolUseProxy/issues/99)です。保存形式、比較条件、問題分類、GitHub Issueへの接続は[実プロジェクト試行の記録・比較・改善提案](../設計/実プロジェクト試行.md)で確定しました。#101〜#105までローカル実装・検証済みです。次は#106の実利用確認です。Issue #107の修正版は導入済みですが、試行記録とGitHubへの自動投稿はまだ無効で、実利用20件の収集は始めていません。コードのcommit・push・公開alphaへの反映も未実施です。

## 完了したこと

- PR #97を`main`へmergeし、`v0.1.0-alpha.12`を公開した
- PR #98で同じtreeを`public-alpha`へ昇格した
- Python 3.11 / 3.12 CI、macOS package smoke、reproducible release candidateが合格した
- fresh Codex Desktop gateは35 / 35 checksがtrueだった
- public side effect 1、static / dynamic protected side effect各0、実行前block各1、raw exposure 0を確認した
- install、update、rollback、disable、remove、data retentionを確認した
- PromptPredictionの新規タスクで、登録済み5件・12チャンク、現在のPreToolUse配送、同一セッションの実行前blockを確認した
- Issue #19と`v0.1.0 Public Alpha` milestoneを完了した

## 現在のGitHub管理

- [Project #1](https://github.com/users/mani1261790/projects/1): Codex対応完了、Detection Qualityフェーズへ更新済み
- [Detection Quality milestone](https://github.com/mani1261790/ToolUseProxy/milestone/2)
- In Progress: #99、#36
- Todo: #24、#54、#38、#46、#37、#39、#55

優先順は次のとおりです。

1. #99: 実project pilot
2. #36: direct / resolved / lineage比較
3. #54 / #24: externalityと動的shell
4. #38: Git push payload
5. #46: local semantic
6. #37 / #39: session・Git境界とreconcile
7. #55: Audit / ML候補

## 次のセッションで最初にすること

1. 現在のbranch（`codex/issue-99-pilot-aggregate`）、`git status`、Issue #99と#101〜#106、Project #1、現在のGoalを確認する
2. [現行研究方針](../研究/現行研究方針.md)と[実装タスク](実装タスク.md)を読む
3. [実プロジェクト試行の設計](../設計/実プロジェクト試行.md)と2つの判断記録を読む
4. 操作評価表はv8、停止確認と見逃し候補はv9、比較位置と範囲確認はv10、送信待ちと同期設定はv11で実装済み。実環境のDBは更新していない
5. 一般向け初期状態では記録せず、Maniの設定でだけ有効にする
6. `hook_monitor/runtime/pilot_models.py`の閉じた項目だけを作り、event IDはSHA-256参照へ置き換える
7. 評価保存失敗が許可・停止を変えず、保護処理失敗は安全側の停止を維持することを試験する
8. 実装済み、試験済み、未確認、次の作業を分けて報告する

第5段階の全体試験は1102件合格、2件スキップ、補助確認1590件合格。その後の追加修正は対象試験51件（1件スキップ）、最終点検14件で確認しました。保存失敗時も保護停止は維持され、初期状態は無効です。実際のGitHub試験投稿は行っていません。

2026-09-06にIssue #107の修正版を現在のCodexへ導入し、5 Hookを個別確認しました。未設定projectの新規Codex実行は無表示・DB追加0件、設定済みprojectの新規Codex実行は`session_start`、`pre_tool_use`、`post_tool_use`、`stop`を同じ実行番号で記録しました。#106ではこの導入済み実体を使い、人工データによる確認と実利用20件を混ぜないでください。公開alphaへ戻すとIssue #107の修正前へ戻るため、共有が完了するまでlocal marketplaceを維持します。

Codex Pluginの再実装、alpha.12 release gate、Desktop Phase Bはやり直しません。新しいCodex版でHook surfaceが変わった場合だけ、回帰として別に扱います。

## 作業時の注意

- repository rootの`protected_sources.json`はToolUseProxyが作成した利用者管理ファイルである。削除、編集、commitをしない
- protected source本文、raw Hook payload、URL、host、absolute pathをIssueやCI artifactへ出さない
- semantic-only、LLM verdict、unknown classificationを自動allowへ昇格しない
- Hookへ届かないHosted Web Search、`write_stdin`継続、未検証nested toolを保護済みと表現しない
- unknownにprotected flowがある場合のfail-closedを維持し、検出精度改善で既存blockを弱めない

## 新しいセッションへの開始文

次の文章をそのまま使えます。

> `/Users/mani/Developer/ToolUseProxy`の開発を引き継いでください。最初にbranch、git status、Issue #99、Project #1、`docs/運用/次セッション引き継ぎ.md`、`docs/運用/実装タスク.md`、`docs/研究/現行研究方針.md`を確認してください。Codex対応とalpha.12 release gateは完了済みです。次はToolUseProxy本体の検出精度改善として、実project pilotのvalue-free集計schemaと問題分類を設計し、具体的な実装計画を提示してください。rootの`protected_sources.json`は利用者管理ファイルなので削除・編集・commitしないでください。`
