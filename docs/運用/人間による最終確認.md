# 人間による最終確認

2026-09-18の利用者指示により、人間の介在が必要な確認は機能実装を進めた後にまとめる。
この一覧の未完了項目は、次の機能の実装を止める条件にしない。
未検証の結果を合格にしたり、確認済みと表示したりしない。

| 対象 | 実装・自動検証 | 最後に人間が確認する項目 |
| --- | --- | --- |
| #188 / PR #192 | project停止、独立した管理者入口、Hook/worker/CLI連携、履歴保持、配布物。1bdabbaのCI全件成功、PR #192をmainへ統合済み（3c899c0） | エージェントから操作できない管理者環境、OSの所有権/ACL、実際の人間承認と取消、故障時の停止・再初期化 |
| #189 / PR #196（統合・Issue完了） | 実際のevents.dbを読み取り専用で追うToolCall・I/O・ブロック判定ビューアー。人工DBへの追記と遅延判定の自動反映を実ブラウザで確認 | ToolUseProxyを人間が再有効化した後、同じ実行の新規ログが表示されること。現時点では無効のため新規収集なし |
| #137 / PR #206（統合済み、Issue未完了） | 人工データ用の単発予測worker、版照合、専用journal、CLI/履歴説明、無効化・復旧。保護判定は変更しない | 同じMac/Codex実行での追加負荷と既存保護の確認。有効性と独立holdoutの証拠は#204で未解決のため、本番接続を合格と扱わない |

| #138（検証中） | 人工比較runner、対象/期限照合、追加停止の無効化、受信証拠の保存/再監査。小規模対照試験では流出減少を示せず、採用不可 | F04の有効性と#204の独立評価、停止利用への明示的な確認。本番へ自動導入しない |

#188の候補は `/private/tmp/tooluseproxy-188-rc-367e530`。旧候補6fa1ff9は期限境界修正前
なので使わない。管理者実機受入手順はPR #192の `docs/運用/Unsetup実機受入.md` にある。
現時点ではToolUseProxyは無効のまま。現在のroot保護リストは読み書きしない。
