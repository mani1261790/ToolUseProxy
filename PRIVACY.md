# v0.2のデータの扱い

ToolUseProxyはToolCallの入力・出力をローカルのevents.dbへ保存します。ログにはコード、ファイル名、秘密情報が平文で含まれる場合があります。暗号化DBではありません。

判定時は、同じプロジェクトの関連するToolCall記録と登録済み情報源のメタデータを、独立した`codex exec`へ渡します。来歴はセッションをまたぐ場合があります。送信内容を確かめるため、Hookは対象ファイルや必要なローカル実行定義を上限付きで読み取ります。Gitの送信では、明示されたローカル参照からコミット・ツリー・ファイル内容を読み取ります。現在のファイルから消えた過去の版も対象になります。取得した証拠が判定入力に含まれる場合があります。保護ファイルも内容一致の索引更新に読み取りますが、登録操作そのものでは本文を読みません。

CLIの保存済み認証を使います。親の会話を暗黙に参照する方式ではなく、必要な記録を明示的にモデルへ送ります。判定用セッションのツール・Hook・Pluginは無効にします。`--ephemeral`はサービス側での処理・保持がゼロであることを意味しません。利用中のCodexのデータ利用条件を確認してください。

新規の初期設定ではこの送信と、バックグラウンド解析によるモデル利用を確認します。既存設定にバックグラウンド解析を自動追加しません。v0.1の「ローカルのみ」という同意を新しい送信への同意として流用しません。判定の失敗・時間切れでは実行を保留し、再試行します。モデル障害が続く間に判定を完了できるとは保証できません。

内容一致索引には鍵付きの指紋を保存します。鍵も同じ専用DB内に置くため、DBを取得した主体に対する暗号化保護ではありません。追加した資源台帳には本文を複製しませんが、元のToolCall記録には含まれ得ます。

ToolUseProxy独自のテレメトリーや改善用アップロードは行いません。旧外部分類worker、旧自動探索・予測・最終回答検査は通常のHookから起動しません。

設定・記録は指定した専用データディレクトリに置きます。POSIXではディレクトリ0700、DB0600を使用します。プロジェクト内のソース、既存登録、古い記録を更新時に削除しません。自動整理は新実行経路に含めません。停止・解除・データ削除は独立した管理者承認の対象です。

ログDB、モデル判定の根拠、スクリーンショットには私的な内容が含まれ得ます。公開IssueやGitには添付せず、人工データで再現してください。
