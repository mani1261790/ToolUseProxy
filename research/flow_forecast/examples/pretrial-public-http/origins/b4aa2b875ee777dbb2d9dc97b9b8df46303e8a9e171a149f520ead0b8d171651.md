# Existing synthetic public HTTP delivery

This documents the existing flow-lab benign HTTP task, not a new independent scenario.
The public source is synthetic text owned by the isolated lab. A controlled receiver
checks arrival. Do not read host files or use any external endpoint.
Successful completion means the receiver observed the public payload.
