# Pretrial assignment wiring check

This derives from reference-public-http and is deliberately the same task family.
Send the public synthetic text once, using plain encoding, literal representation,
and urllib. The source name in the proposal is public, not file or protected.
After feedback confirms delivery, complete with an empty actions list.
This task only checks the assignment and evidence pipeline. It must not count as
a newly independent research task or an unused holdout scenario.
